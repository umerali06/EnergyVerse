# FEV Testing and Micro-Task Protocol

## Mandatory Workflow

1. Build one micro-task at a time.
2. Each implementation micro-task must include its frontend, backend, and database work and must be real and functional.
3. Test the specific screen, page, endpoint, and relevant data behavior created by that micro-task.
4. Show the product owner the focused test result.
5. Only after successful testing and evidence is shown, mark the micro-task **Done** in `PHASE_TRACKER.md`.
6. After marking it Done, update `ARCHITECTURE.md` with how the completed slice connects to other slices.
7. Do not start the next micro-task until the current one is tested, marked Done, and documented.
8. Never assume requirements. Stop and ask when a requirement or consequential choice is unclear.
9. Update `PHASE_TRACKER.md` and `DECISIONS.md` every session so progress, remaining work, and blockers stay current.

## Per-Micro-Task Test Record

For every micro-task, record:

- Micro-task identifier and acceptance criteria
- Components changed: frontend, backend, database, and infrastructure where applicable
- Automated checks run and their exact results
- Focused manual scenario tested, including offline behavior where applicable
- Tenant isolation and API authorization checks where applicable
- Permission/UI visibility checks where applicable
- Audit-log checks for critical actions where applicable
- AI confirmation/override checks where AI is involved
- Sync, conflict, retry, and recovery checks for offline workflows where applicable
- Defects or limitations discovered
- Evidence shown to the product owner
- Final status and completion date

## Completion Gate

A micro-task is not Done merely because code was written. Done requires passing focused verification, sharing the result, updating the phase tracker, and recording the slice’s architectural connections.

Phase 0.1 contains documentation only. Its focused verification is checking that all requested context files exist, contain the supplied constraints, and preserve open decisions without inventing answers.

## Phase 0.2 Evidence

| Micro-task | Acceptance and focused evidence | Result | Date |
|---|---|---|---|
| 0.2 — monorepo scaffold | API: `poetry install` completed; `poetry run ruff check .` → `All checks passed!`; `poetry run mypy app` → no issues in 4 source files; `poetry run pytest` → 2 passed; Uvicorn served `GET /` with HTTP 200 and `{"service":"fev-api","status":"ok"}`. Admin: pnpm install completed; `pnpm lint` → no warnings/errors; `pnpm build` compiled and generated 4 static pages; `pnpm dev` became ready and served `/` with HTTP 200. Mobile: `flutter pub get` completed; `flutter analyze` → no issues; `flutter build web` succeeded; Flutter web-server served `/` with HTTP 200. CI: GitHub Actions workflow `CI` run 29414241787 passed all three jobs — API (24s), Admin (42s), Mobile (48s). PR: https://github.com/umerali06/EnergyVerse/pull/1. Tenant isolation, authorization, audit, AI, and offline-sync checks are not applicable to this scaffold-only slice. | Passed | 2026-07-15 |
| 0.3 — Firebase Admin and health pipe | API CI-safe checks: `poetry check --lock` passed; Ruff passed; mypy reported no issues in 9 source files; pytest reported 10 passed and 1 credential-dependent skip. Tests cover all three health statuses, valid timestamp shape, four local CORS origins, Firestore error conversion, and the root route. Live checks against Firebase project `thinking-case-469504-c0`: the integration test passed with `GOOGLE_APPLICATION_CREDENTIALS` and separately with in-memory `FIREBASE_CREDENTIALS_B64`; Uvicorn served `GET /health` as HTTP 200 with `firestore: connected`; no-credential execution returned HTTP 200 with `firestore: unconfigured`. Admin lint/build passed and a headless browser rendered `API: connected · Firestore: connected`. Flutter analyze, widget test, and web build passed; the built web app served HTTP 200 and a headless-browser screenshot visibly showed the same connected indicator. CORS preflights for admin port 3000 and Flutter port 8080 returned their requested allow-origin. `infra/firebase/firestore.rules` denies every client read/write; deployment instructions are documented but rules deployment was not part of this slice. GitHub Actions run 29422051072 passed API (28s), Admin (35s), and Mobile (50s). PR: https://github.com/umerali06/EnergyVerse/pull/2. No tenant records, auth/permissions, audit events, AI, or offline-sync behavior exists yet, so those checks are not applicable. | Passed | 2026-07-15 |
| 0.4 — tenant data and RBAC foundation | CI-safe API suite: Ruff passed; strict mypy passed for 29 `app`/`scripts` source files; pytest collected 18 tests and reported 16 passed with 2 real-credential integration tests skipped. Coverage includes repository CRUD for all mutable collections plus append/read for immutable audit logs; exact base-contract fields and exceptions; exact 16-key permission catalog; hard-coded seven-role matrix and immutable resolution; mutation audit shape; deterministic seed idempotency; and `test_company_a_query_never_returns_company_b_documents`. The real seed against `thinking-case-469504-c0` produced `companies=2`, `permissions=16`, `roles=8`, `role_permissions=86`, `users=8`, `audit_logs=104`; an optimized second run returned the identical counts without new audit records. The real credential-gated isolation/RBAC test passed in 39.11s, verifying cross-company reads return no user and all seven Acme roles resolve. Admin lint/build and Flutter analyze remained green. GitHub Actions run 29426235503 passed API (30s), Admin (34s), and Mobile (40s). PR: https://github.com/umerali06/EnergyVerse/pull/3. Firestore client rules remain deny-all. No auth flow, HTTP data route, UI, feature collection, AI, or offline-sync behavior was introduced, so those checks are not applicable. | Passed | 2026-07-15 |
| 0.5 — backend auth foundation | CI-safe verification: Poetry install had no dependency changes; Ruff passed; strict mypy reported no issues in 36 app source files; pytest collected 34 tests and reported 31 passed with 3 real-credential skips. Coverage includes missing/invalid/expired/revoked token 401 responses; active `/me` identity with the exact field-inspector permission matrix; inactive and missing users; exact claims synchronization; provisioning with Firestore user, custom claims, and audit evidence; optional/default action-link settings; seed-only UID migration, timestamp preservation, interrupted-run recovery, no-placeholder recreation, and double-run idempotency. Real project `thinking-case-469504-c0`: auth seed counts were `companies=2`, `permissions=16`, `roles=8`, `role_permissions=86`, `users=8`, `audit_logs=112`; the credential-gated password-login integration passed in 81.35s. Uvicorn booted and `/health` returned Firestore `connected`; a real Identity Toolkit email/password token produced `/api/v1/auth/me` HTTP 200 with `company_id=acme-energy`, `role_key=field_inspector`, and its exact 9 permissions; a malformed token returned HTTP 401; Firebase Admin returned claims `company_id`, `role_id`, and `role_key`. GitHub Actions run 29472291158 passed API (24s), Admin (35s), and Mobile (54s). PR: https://github.com/umerali06/EnergyVerse/pull/4. Firestore rules remain deny-all. Login UI and per-permission API/UI gating were intentionally not introduced. | Passed | 2026-07-16 |
| 0.6 — RBAC enforcement and client guards | API: Poetry install passed; Ruff passed; strict mypy reported no issues in 41 source files; pytest collected 63 tests and reported 60 passed with 3 real-credential skips. Focused tests prove permission present→200, absent→exact top-level 403/missing list, `all` missing-one denial, `any` one-present allow, unauthenticated→401, denial audit shape, role helper behavior, and invalid factory configuration. The hard-coded route matrix passed all 21 cases: Super Admin `allow/allow/allow`; Company Admin `allow/allow/allow`; Operations Manager `allow/allow/allow`; Field Inspector `deny/deny/deny`; Maintenance Technician `deny/deny/allow`; HSE Manager `deny/deny/allow`; Executive `deny/deny/deny` for single/assets.write, all/assets.write+reports.generate, and any/permits.approve+work_orders.write respectively. Real project proof: Uvicorn booted; an Operations Manager password token returned HTTP 200 and `{"ok":true}` on the single gate; a Field Inspector token returned the exact HTTP 403 body with missing `assets.write`; no token returned the uniform HTTP 401 body; the real scoped audit record contained `action=access.denied`, the demo route, required/missing keys, and `mode=all`. Admin: Vitest passed 3 logic/render tests, lint/build passed, and Next dev served HTTP 200 with the RBAC proof plus `No access: assets.write required`. Mobile: analyze reported no issues; the focused suite passed 4 tests covering predicates, one `/me` load, allowed rendering, and no-access rendering; the full CI widget suite passed. GitHub Actions run 29482580541 passed API (26s), Admin including tests/build (38s), and Mobile including analyze/tests (1m1s). PR: https://github.com/umerali06/EnergyVerse/pull/5. Firestore rules remain deny-all; no cross-tenant bypass, feature module, login UI, or Phase 0.7 code was introduced. | Passed | 2026-07-16 |
| 0.7 — shared design system | Shared tokens: `node packages/design-tokens/scripts/generate.mjs` regenerated admin TypeScript/CSS and Flutter Dart bindings; `git diff --check` passed. Admin: Vitest reported 2 files and 8 tests passed, including every primitive/state, persisted dark/light switching, focus/ARIA smoke, toast behavior, and reduced-motion rendering; ESLint reported no warnings/errors; Next production build compiled and generated 5 static pages. The development server served `/design-system` as HTTP 200 and rendered `FEV Design System`, `Industrial energy primitives`, and `DEV-ONLY SHOWCASE`; production code retains a development-only route guard. Mobile: `flutter analyze` reported no issues; the complete suite reported 11 tests passed, covering dark/light token resolution, primitive/accessibility states, modal/toast feedback, persisted theme choice, zero-duration reduced motion, and the showcase rendered under both themes; `flutter build web` succeeded with the locally bundled fonts. Motion uses the shared fast/standard/slow tokens for press/hover, fade-slide, stagger, modal, and shimmer and respects each platform's reduced-animation signal. GitHub Actions CI run 29493608846 passed all three jobs: API lint/type/tests, Admin lint/tests/build, and Mobile analyze/tests. PR: https://github.com/umerali06/EnergyVerse/pull/6. No feature, auth, dashboard, backend, database, tenant, audit, AI, or offline-sync behavior was introduced. | Passed | 2026-07-16 |
| 0.8 — API contract, generated clients, unified errors, and toast infrastructure | Backend: `poetry check --lock`, Ruff, and strict mypy passed for 44 source files; pytest reported 65 passed and 3 intentional credential skips. Error tests cover validation 422, authentication 401, authorization 403 with `required`/`missing` under `details`, not-found 404, conflict 409, sanitized 500, UUID generation/echo, and all operation IDs. OpenAPI export plus pinned `typescript-fetch`/`dart-dio` regeneration produced `CONTRACT_DRIFT_CHECK=clean`; generated Dart analyze found no issues and all 22 generated tests passed. Admin: ESLint had no warnings/errors, Vitest reported 11 passed with 2 credential-only skips, and Next production build compiled all routes. Mobile: analyze found no issues, all 14 service/widget tests passed, and the web release build succeeded. Real project proof against `thinking-case-469504-c0`: Firebase password login and backend `/me`/claims integration passed; Uvicorn `/health` returned HTTP 200 with `firestore=connected`; the generated TypeScript client returned typed field-inspector `/me` with the exact 9 permissions, while a malformed real request produced a typed 401 envelope, UUID `request_id`, toast callback, and 401 hook. GitHub Actions run 29515683270 passed API (27s), Admin (49s), Mobile (1m37s), and Contracts drift (2m29s). PR: https://github.com/umerali06/EnergyVerse/pull/7. Firestore rules and the Phase 0.4 tenancy boundary are unchanged; no feature screen/module or Phase 1 code was introduced. | Passed | 2026-07-16 |
| 1.1 — Firebase client login | Admin: the complete Vitest run reported 22 passed with 3 credential-only skips; the focused login suite passed all 11 validation, loading, success, friendly-error, inactive-user sign-out, restore, sign-out, and reduced-motion cases. ESLint had no warnings/errors and the Next production build compiled all routes. Mobile: all 25 API/design/auth/widget tests passed; `flutter analyze` reported no issues; the release web build succeeded. Real project `thinking-case-469504-c0`: Uvicorn `/health` returned HTTP 200 with `firestore=connected`; a seeded Field Inspector signed in with Firebase email/password in both the admin and Flutter web UIs, each called typed `/api/v1/auth/me`, and each rendered `company=acme-energy`, `role=field_inspector`, and the exact 9-permission Phase 0.4 set. Dark login and authenticated Home evidence was captured for both clients. A real wrong password rendered the non-enumerating `Invalid email or password` inline error and critical toast; no password or token was logged or committed. GitHub Actions run 29537912656 passed API (27s), Admin (45s), Mobile (1m42s), and Contracts drift (2m26s). PR: https://github.com/umerali06/EnergyVerse/pull/8. Firestore rules, server RBAC, tenancy, and feature collections are unchanged; signup/verification, reset, and full session/route hardening remain deferred. | Passed | 2026-07-17 |
| 1.2 — organization signup and email verification | Backend CI-safe verification: Ruff passed, strict mypy found no issues in 43 source files, and pytest reported 72 passed with 4 opt-in real-credential skips. Tests cover generated company IDs with duplicate display names, seven-role seeding, exact company-admin permissions, tenant isolation, audit shape, email conflicts, `/me` 200 for unverified identity, and `require_verified_email` unified 403 enforcement. Admin: 25 tests passed with 3 real-only skips, including signup validation/provisioning, unverified routing, verification delivery/cooldown, restore/errors, and reduced motion; lint and production build passed. Mobile: all 29 service/widget tests passed, analyze reported no issues, and the web release build succeeded. The OpenAPI export and pinned TypeScript Fetch/Dart Dio clients were regenerated; CI's drift job passed. Real project `thinking-case-469504-c0`: registration created `cmp_feee017b83914cdd8323745e6359cc32`, seven system roles, and `umeraliumeralimalik+fev-20260717160752@gmail.com` as company admin; Firebase dispatched and the owner received/confirmed the verification email. A fresh password sign-in then returned `/me` HTTP 200 with `email_verified=true`, `role_key=company_admin`, and the exact 15-permission matrix. Real Firestore health, tenant isolation, auth/claims, and generated TypeScript client tests passed; the latter also proved real 401 envelope/toast/request-ID handling. GitHub Actions run 29576897928 passed API (25s), Admin (52s), Mobile (1m17s), and Contracts drift (2m13s). PR: https://github.com/umerali06/EnergyVerse/pull/9. Invite onboarding, password reset, and comprehensive session guards remain deferred; Firestore direct-client rules remain deny-all. | Passed | 2026-07-17 |
| 1.3 — forgot / reset password | Admin CI-safe verification: the full Vitest run reported 31 passed with 3 credential-only skips; the forgot-password suite proves empty/invalid email validation blocks the gateway call, the loading state, the neutral confirmation screen, `auth/user-not-found` rendering the identical neutral success (enumeration parity), exact `too-many-requests`/`network-request-failed` messages, the enforced 60-second resend cooldown, and reduced-motion rendering; ESLint reported no warnings/errors and the Next production build compiled all routes. Mobile: all 35 tests passed, including the mirrored validation/loading/neutral-confirmation/enumeration-parity/error-mapping/cooldown/reduced-motion widget cases; `flutter analyze` reported no issues. Real project `thinking-case-469504-c0`: the new credential-gated reset suite in `apps/admin/src/auth/auth.integration.test.ts` passed — a real Firebase reset email was dispatched to the Phase 1.2 company admin `umeraliumeralimalik+fev-20260717160752@gmail.com` (3.20s), and an unknown mailbox behaved indistinguishably from success (3.00s), proving no account enumeration at the provider boundary. Sending a reset changes no password until the emailed link is used, so seeded credentials are unaffected; completing the change happens in Firebase's hosted flow as a manual owner step (documented in the README). Forgot-password and confirmation screenshots for both clients were captured under `docs/evidence/phase-1.3/` (kept local per the evidence convention). GitHub Actions run 29643620477 passed API (28s), Admin (48s), Mobile (1m29s), and Contracts drift (2m19s). PR: https://github.com/umerali06/EnergyVerse/pull/10. Backend notification delivery, a custom reset page, and session/refresh/route-guard hardening remain deferred to their scheduled slices. | Passed | 2026-07-18 |
| 1.4 — session management and route guards | Admin CI-safe verification: 41 Vitest tests passed with 6 credential-only skips. The suite proves session restore behind the splash with no login flash and a single `/me` resolution; a dead session expiring with one session-expired toast, Firebase sign-out, and a login redirect; client-layer 401 handling (one forced-refresh retry succeeding, post-refresh 401 signing out once, failed refresh skipping the retry) with no generic toast on 401s; an unauthenticated deep link to `/rbac-demo` redirecting to `/login?next=%2Frbac-demo` and returning there after login; the branded 403 page (missing `assets.write`) versus the allowed role passing the gate; authenticated users redirected away from login; unverified users held at verify and blocked from protected routes; logout plus back-navigation revealing no protected content; `refreshSession()` re-resolving `/me` with new permissions; signup/verify/forgot cooldown regressions; and reduced motion. ESLint reported no warnings and the production build compiled all eight routes. Mobile: 43 tests passed covering the mirrored guard matrix (splash restore, expiry feedback, pendingRoute return-to, 403 fallback, redirect-away, unverified blocking, stack-clearing logout, refresh-session permission updates) plus Dio-level 401 retry/expiry interceptor tests; `flutter analyze` reported no issues. API: pytest 72 passed with 4 credential skips — `require_permission`/`require_verified_email` gates are unchanged from 0.6/1.2. UI evidence with the real Firebase project `thinking-case-469504-c0` (seeded Field Inspector client sign-in against a local API stand-in, since no Admin service-account credential was present this session): deep-link redirect, post-login return to the branded 403, restore splash then Home, and forced 401s producing exactly one session-expired toast and a clean login redirect — screenshots kept locally under `docs/evidence/phase-1.4/`; the browser run also exposed and fixed a double-toast expiry bug. Live-API verification (2026-07-18, owner-supplied service account): Uvicorn booted with `firestore=connected`; the credential-gated session suite in `auth.integration.test.ts` passed — the seeded Field Inspector signed in, `/me` resolved identically before and after a forced ID-token refresh, and `/api/v1/_rbac-demo/single` returned the authoritative HTTP 403 for the missing `assets.write` (37.10s); the seeded login-through-`/me` test passed (11.25s); and the generated-client real chain passed (9.39s), including a real 401 envelope with UUID `request_id` now handled by the unauthorized hook without a generic toast — that credential-gated expectation was updated to the 1.4 policy. An initial run hit Firebase `too-many-requests` rate limiting from back-to-back sign-ins; run the two integration files separately. GitHub Actions run 29651046645 passed API (25s), Admin (49s), Mobile (1m39s), and Contracts drift (2m9s). PR: https://github.com/umerali06/EnergyVerse/pull/12. Server-session cookies, SSR-aware guards, invite onboarding, and Phase 2 feature routes remain deferred. | Passed | 2026-07-18 |
| 2.1 — application shell | Backend: Ruff passed, strict mypy reported no issues in 46 source files, and pytest reported 73 passed with 3 credential skips — `/me` now returns `company_name` (loaded from the scoped company document; a missing company is HTTP 403) and the contract test plus RBAC fixtures cover it; OpenAPI and the pinned TypeScript Fetch / Dart Dio clients were regenerated for the drift job. Admin CI-safe verification: 55 Vitest tests passed (6 credential-only skips), including table-driven nav visibility for company_admin, operations_manager, field_inspector, and executive cross-checked against the 0.4 matrix (all non-admin system roles legitimately differ only by Admin & Settings because every role holds all `.read` keys, so a minimal custom `safety.read`-only set additionally proves each gate); nav click routing with active-state and Coming-soon landings; sidebar collapse persisting through localStorage across renders; the tablet-forced icon rail; the mobile drawer's focus trap, Escape-close, and close-on-route-change; user-menu identity (name, email, role badge, Acme Energy) with sign-out preserving 1.4 behavior; disabled search/notifications placeholders; skip link, banner/nav/main landmarks, and keyboard traversal; branded 404 inside the shell; and reduced motion. ESLint reported no warnings and the production build compiled all 16 routes. Mobile: 51 tests passed covering the mirrored table-driven bottom-nav/More filtering, tab routing with selected-state, More-sheet navigation, 404-in-shell, user-menu identity and actions, theme toggle, and all updated 1.x regression suites; `flutter analyze` reported no issues. UI evidence (real Firebase client sign-in against a role-switchable local `/me` stand-in, screenshots under `docs/evidence/phase-2.1/`): desktop shell for company_admin (all nine modules across four groups) versus field_inspector (no Administration group), sidebar expanded and collapsed, tablet rail, mobile layout with open drawer, the user menu, and the Assets Coming-soon page. GitHub Actions run 29681158702 passed API (27s), Admin (59s), Mobile (1m36s), and Contracts drift (2m19s). PR: https://github.com/umerali06/EnergyVerse/pull/15. Dashboard content/KPIs (2.2), the widget framework (2.3), real search (16), and notifications (15) remain deferred. | Passed | 2026-07-19 |
| 2.1b — design language elevation | Restyle-only pass verified by the unchanged suites: admin 55 Vitest tests passed (6 credential-only skips) plus ESLint clean and a full production build of all 16 routes on the new tokens/fonts; mobile 51 tests passed and `flutter analyze` reported no issues; backend and contracts untouched. Contrast audit: 14 foreground/background pairs (primary/secondary/muted text on all three surface layers, button labels, active nav, status pills on tinted fills, both themes) all meet WCAG AA — the round-2 fixes moved light-mode active nav to primary-600, the login CTA to the ember accent (its single primary action), and dark muted text to #8A9BB5. Two screenshot-led refinement rounds via headless Chrome against the live dev server: round 1 exposed AA failures and a still-generic showcase; round 2 verified the fixes plus the instrumentation-label idiom, asymmetric demo rows, and mono machine values. Evidence under `docs/evidence/phase-2.1b/` (local-only): before/after showcase dark+light+mobile, shell desktop/tablet/mobile/drawer/user-menu for two roles, final dark+light shell after fixes. Fonts are Latin-subset (~70% smaller) and self-hosted in both clients. GitHub Actions run 29695445201 passed API (27s), Admin (59s), Mobile (2m19s), and Contracts drift (2m14s). PR: https://github.com/umerali06/EnergyVerse/pull/16. | Passed | 2026-07-19 |
| 2.1c — brand identity, SEO, theming enforcement | Brand palette re-derived from the official logo — sampled orange #FB4402 and navy #002865 (recorded in D-017) — as OKLCH 50–900 scales; the contrast re-audit caught and fixed real regressions (dark critical text 2.90:1, light warning 2.17:1, light info 2.89:1, dark primary-button label 4.24:1, and a dropped accent-ink token rendering the CTA label white-on-orange at 3.4:1): statusStrong/statusSoft token sets plus theme-aware action colors now hold every checked pair at WCAG AA in both themes. Admin: 67 Vitest tests passed (12 new covering public-route canonical/OG/Twitter/index, protected-route noindex with unique titles, robots/sitemap/manifest correctness, and the theme-aware Logo component incl. decorative aria-hidden), ESLint clean, production build green, and the new CI bundle-budget step passed (342.2 KB shared bundle vs 430 KB budget). Planted-violation proof: a raw #FF0000 in app-shell.tsx failed lint with the token-guard message and was removed. Mobile: 52 tests passed (incl. the new token-guard source scan) and flutter analyze clean; the theme contract test now pins dark=primary400 / light=primary800. Token-propagation proof: a single tokens.json edit (primary-800 → #0B7A3E) regenerated into both the admin CSS/Tailwind bindings and DsColors.primary800, screenshot captured, then reverted. Lighthouse on the production login (headless, local 4× throttle): Performance 56, Accessibility 100, Best Practices 100, SEO 100 — recorded as the regression baseline; the login logo is eager/high-priority for LCP. Evidence under docs/evidence/phase-2.1c/ (local-only): branded login/shell/showcase dark+light, collapsed-sidebar mark, token-proof screenshot. GitHub Actions run 29705032577 passed all four jobs. PR: https://github.com/umerali06/EnergyVerse/pull/17. | Passed | 2026-07-19 |
| 2.2 — dashboard content and chart infrastructure | Backend: Ruff and strict mypy passed; pytest reported 81 passed with 4 credential-only skips, including the 9 new dashboard tests (real tenant counts, `reports.read` enforcement, unverified-email block, cross-tenant isolation on both summary and activity, cursor pagination walking 25 events across 3 pages with no overlap and no repeats, action-filter plus actor-name enrichment, an invalid cursor's exact `invalid_cursor` 422, and a 7-day series that zero-fills every day with no events and sums correctly against a mixed 1-day/3-day-old event set); the new `firestore.indexes.json` composite index (`audit_logs`: company_id ASC, created_at ASC) was committed as IaC. A CI-only regression surfaced and was fixed: `require_permission`'s dependency graph resolves `get_access_denial_audit` even on the success path, which the initial fixture didn't override, so 8 of 9 new tests passed only locally (a real service account was present) and failed in CI; the fixture now overrides that dependency the same way `test_rbac_enforcement.py` already did, verified locally with the credential env vars unset to reproduce CI exactly (81 passed/4 skipped). Admin: the dashboard suite added 12 Vitest tests (loading/empty/error states for summary, series, and activity; 7/30/90-day window switching re-fetching all three; load-more pagination appending without duplicating; the reserved-KPI tiles rendering their honest empty state gated by their own future permission; and role-based visibility of the quick-actions card) plus 2 new `api/client.test.ts` cases for a real summary success and a unified-envelope error/toast; full suite, ESLint, and production build stayed green. Mobile: `dashboard_screen_test.dart` added 12 widget tests covering the same state/window/pagination/reserved-KPI/role matrix, using a custom manual-drag scroll helper after `scrollUntilVisible` proved flaky against the app's own `ListView`; `flutter analyze` stayed clean. A real bug was found and fixed during evidence review: the activity feed rendered a double slash (`route//api/v1/...`) for `access.denied` events because the audit's `target_id` already starts with `/`; a shared `formatTarget()` helper in both clients now checks for the leading slash before joining. UI evidence (screenshots under `docs/evidence/phase-2.2/`, local-only): populated and fresh-tenant-empty dashboard, both themes, desktop and mobile, captured via a local role/state-switchable stub API since the real tenant's dashboard queries require the new composite index. Real-Firestore check: partially blocked — the connected project (`thinking-case-469504-c0`) does not yet have the composite index provisioned (the service account also lacks index-admin IAM to create it directly), so the end-to-end real-creds dashboard query is deferred until the index is deployed via `firebase deploy --only firestore:indexes` or the Firestore console's auto-create link; the fix (IaC index definition) is committed and this gap is the only known follow-up. GitHub Actions run 29813668477 passed all four jobs. PR: https://github.com/umerali06/EnergyVerse/pull/18. | Passed | 2026-07-21 |
| 3.1 — company-scoped user management | Prerequisite closed first: the Phase 2.2 Firestore composite index (`audit_logs`: company_id ASC, created_at ASC) was actually deployed to `thinking-case-469504-c0` via `firebase deploy --only firestore:indexes`, resolving that phase's deferred real-creds gap. Backend: Ruff and strict mypy passed; pytest reported 105 passed with 3 credential-only skips, including 23 new tests across `test_users.py` covering tenant isolation on list/get, `users.manage` enforcement, real field/status filters and search, cursor pagination across 27 users with no overlap, invite creating a real-shaped Firebase Auth user + Firestore doc + claims + `user.provisioned` audit, duplicate-invite 409, foreign-company-role and Super-Admin-role rejection on both invite and update, role-change syncing claims via `sync_claims_from_role`, and all four safety rules (self-deactivate 409, self-demote-out-of-admin 409 proven against a seeded second admin so the last-admin rule can't mask it, last-active-admin-cannot-be-deactivated/demoted 409, and a second-admin successfully deactivating the first as the count-based control case) — plus 2 new tests for the supporting read-only `GET /api/v1/roles` (excludes `super_admin`, tenant-scoped). OpenAPI export and the pinned TypeScript Fetch / Dart Dio clients were regenerated; the contracts job passed with zero drift. Admin: 91 Vitest tests passed (10 new in `users-page.test.tsx` covering loading/empty/error states, cursor pagination, live search, invite validation and success including the real `sendUserInviteEmail` call, role-change updating the displayed effective permissions, and the deactivate confirmation flow) plus the app-shell nav-visibility table updated for the new company_admin-only "Users" item; ESLint clean; production build compiled all 21 routes; bundle budget held at 342.2 KB (unchanged). One real timing bug was found and fixed during test-writing: `UserDetailModal`'s fetch effect depended on the whole `useUsersData()` return object, which changes identity on every render, so any unrelated list refresh while the modal was open re-fetched and clobbered a just-saved edit — fixed by depending only on `userId` and passing the three specific stable callbacks (`getUser`/`updateUser`/`setUserStatus`) as props instead of the whole hook object. Mobile: `flutter analyze` stayed clean and `flutter test` reported all 72 tests passed, including 6 new `users_screen_test.dart` cases (loading/empty/error, pagination, detail-sheet permissions, and the route-level 403 for a non-admin role) plus the `ApiContract` fakes updated across five existing test files for the two new read-only methods. Real-Firestore evidence against `thinking-case-469504-c0` (screenshots under `docs/evidence/phase-3.1/`, local-only): the users list in both themes showing all seven real seeded roles; the invite modal empty and filled; the post-invite list showing a genuinely created "Alex Rivera" user with real Firestore/Firebase-Auth/claims/audit records; the user-detail modal showing that user's real resolved Maintenance Technician permission set; and the branded 403 for `field_inspector`, whose sidebar correctly omits the entire Administration group. The test invite was deleted (Firebase Auth user + Firestore doc) after each capture pass, leaving the real tenant exactly as found. The final leg of the real-creds check — completing the password-reset link and signing in as the invited user — was scripted against the Identity Toolkit REST API (generate the same reset link the invite flow sends, redeem it, sign in, call `/api/v1/auth/me`) but hit Firebase's daily "Get OOB Code" quota after repeated evidence iterations in this session; this is a quota exhaustion from repeated local testing, not a product defect, and is recorded here rather than faked — the mechanism itself (D-021) is otherwise fully exercised by the successful invite-creation and email-dispatch calls. GitHub Actions run 29854169703 passed all four jobs. PR: https://github.com/umerali06/EnergyVerse/pull/19. Role/permission editing (3.2), company settings (3.3), the audit viewer (3.4), and Super-Admin cross-tenant management (3.5) remain deferred. | Passed | 2026-07-21 |
| 3.2 — roles and permission management | Backend: Ruff and strict mypy (51 source files) passed; pytest reported 126 passed with 3 credential-only skips, including 33 new tests in `test_roles.py` covering tenant isolation on list/get (`super_admin` hidden, cross-tenant 404), the list endpoint accepting either `users.manage` or `roles.manage`, permission/user counts on the list, create rejecting unknown permission keys (422) and `platform.admin` (403) and duplicate names (409, case-insensitive), clone copying the source's permission set exactly, system-role update/delete both returning 409 `system_role_locked`, the permission-based last-holder guard blocking a `users.manage` removal that would leave zero active holders and allowing it when a second holder remains (proven by first deactivating the seeded Company Admin and Super Admin, mirroring 3.1's second-admin control-case pattern), delete blocked with an exact assigned-user count when a role has occupants and succeeding once unassigned, batched claims sync populating `role_id`/`role_key` for every current holder of an edited role, and the audit log's `role.permissions_updated` entry containing the exact before/after/added/removed key lists — plus the existing `test_users.py` role-list tests were moved into the new file and the DI wiring switched from a `roles.py`-internal `RoleRepository` monkeypatch to a proper `get_role_management_service` override. A new `GET /api/v1/permissions` route (grouped, ordered catalog) and `RoleDeleted`/`CreateRoleRequest`/`UpdateRoleRequest`/`RoleDetail`/extended `RoleSummary` models were added with no Firestore schema change. OpenAPI export and the pinned TypeScript Fetch / Dart Dio clients were regenerated; the contracts drift job passed clean. Admin: 103 Vitest tests passed (11 new in `roles-page.test.tsx` covering loading/empty/error states, blank and cloned role creation, the permission matrix omitting the entire `platform` group, a system role rendering fully read-only, the impact-warning + added/removed diff card gating a save when users are assigned, an immediate save when none are, and the delete-blocked state linking to the filtered users list) plus a new `Checkbox` design-system primitive (with its own interaction test) and the app-shell nav-visibility table updated for the new company_admin-only "Roles" item; ESLint clean; production build compiled all 24 routes; bundle budget held at 342.2 KB (unchanged, the new route lazy-loads under `/roles`). Mobile: `flutter analyze` reported no issues and `flutter test` reported all 78 tests passed (5 new in `roles_screen_test.dart` covering loading/empty/error, the role-detail sheet's permission-key list, and the route-level 403 for a role without `roles.manage`), plus the `ApiContract` fakes updated across six existing test files for the two new read-only methods; CI ran the complete suite in one process, while local verification this session ran files in smaller batches after repeated headless-browser evidence runs left this dev machine memory-constrained for a single combined `flutter test` invocation — an environment artifact of this session, not a product defect, and every file passed individually and in CI. Real-Firestore proof against `thinking-case-469504-c0` (no UI, scripted directly against the running API and Identity Toolkit REST endpoints to avoid any UI-timing flakiness): signed in as the seeded `company_admin`, created a custom role holding `reports.read`+`safety.read`, reassigned the seeded `executive` demo user onto it via `PATCH /api/v1/users/{id}`, confirmed that user's `/me` now resolved exactly `{reports.read, safety.read}`, edited the role's permissions to `{reports.read, reports.generate}`, forced a Secure Token API refresh for that user's ID token, confirmed `/me` on the refreshed token now resolved exactly `{reports.read, reports.generate}`, then restored the user to their original system role and deleted the probe role — the tenant was left exactly as found. UI evidence (screenshots under `docs/evidence/phase-3.2/`, local-only, both themes for the top-level list/403 states): the roles list showing all seven real seeded system roles with real permission/user counts; the create-role permission matrix mid-selection (grouped, group-level "select all", live count, `platform` group absent); a role with one real assigned user in the delete-blocked state; the impact/diff confirmation card (`+ safety.read`, `− reports.read`, "1 user currently hold this role"); the Company Admin system role rendered fully read-only with all 15 permissions and no Edit/Delete actions; and the branded 403 for `field_inspector`. GitHub Actions run 29892973902 passed all four jobs (Contracts 2m28s, Mobile 1m48s, API 33s, Admin 1m23s). PR: https://github.com/umerali06/EnergyVerse/pull/20. Company settings (3.3), the audit viewer (3.4), and Super-Admin cross-tenant management (3.5) remain deferred. | Passed | 2026-07-22 |
| 3.3 — company profile, branding, and settings | Backend: Ruff and strict mypy (60 source files) passed; pytest reported 141 passed with 3 credential-only skips, including 15 new tests in `test_company.py` covering tenant isolation and counts on `GET /api/v1/company`, 403 without `company.settings`, a profile update validating and auditing before/after, 422 for an unrecognized industry/an unrecognized IANA timezone/a malformed BCP-47 locale, explicit-`null` clearing of `industry`/`contact_email`/`contact_phone` while untouched fields (name, timezone, locale) survive the clear, a field omitted from one PATCH staying untouched by a later PATCH, 422 rejection of an explicit `null` for the three fields that can never be cleared (name/timezone/locale), logo upload rejecting the wrong content-type and an oversized file (422 both), a valid upload storing at the fixed `companies/{id}/branding/logo` path and auditing `company.logo_updated`, a second upload overwriting the same path rather than creating a new blob, and removal clearing the reference and auditing `company.logo_removed`. A real bug surfaced while writing these tests and while probing the live project (not by inspection): `CompanyRepository.update`'s `without_none()` filter meant an explicitly-`null` field was indistinguishable from an omitted one, so "Not set" for industry silently did nothing — fixed by switching to Pydantic's `exclude_unset` semantics (see D-028's session-review entry). New `app/company/` (service + a small validated `INDUSTRY_CHOICES`/timezone/locale constants module) and `app/storage/` (`CompanyLogoStorage`, the Admin SDK bucket wrapper) packages, a new `tests/fakes/storage.py` in-memory bucket standing in for real GCS, and `CurrentUser` gained defaulted `company_timezone`/`company_locale` fields populated in `get_current_user` alongside the existing `company_name` line. `python-multipart` and `tzdata` were added as explicit dependencies (the latter because Windows has no system IANA database for `zoneinfo` to read locally — confirmed by a real local failure before the fix). OpenAPI export and the pinned TypeScript Fetch / Dart Dio clients were regenerated for `CompanyProfile`/`UpdateCompanyRequest`/`CompanyApi`; the contracts drift job passed clean. Admin: 111 Vitest tests passed (8 new in `company-settings-page.test.tsx` covering loading/retry-capable-error states, the branded 403 for a role without `company.settings`, dirty-state tracking through a save/discard/re-edit/save cycle, client-side rejection of a too-short company name, a successful logo upload with its preview and a follow-up removal, a too-large logo rejected before upload, and a wrong-type logo rejected via drag-drop specifically — `userEvent.upload()` itself enforces the file input's `accept` filter, so a mismatched-type file never reaches the change handler that way, unlike a real drag-and-drop); a new `logo-upload.tsx` drag-drop component (no prior upload pattern existed anywhere in the admin app) and a jsdom `URL.createObjectURL`/`revokeObjectURL` polyfill were added to the shared test setup; ESLint clean; production build compiled all 24 routes (`/settings` no longer the placeholder `ComingSoonScreen`); bundle budget held at 342.2 KB. Mobile: `flutter analyze` reported no issues and `flutter test` reported all 81 tests passed (3 new in `company_profile_screen_test.dart` covering loading/retry-capable-error states and the route-level 403 for a role without `company.settings`), plus the `ApiContract` fakes updated across seven existing test files for the one new read-only method; a new `timezone` package dependency lets `apps/mobile/lib/dashboard/format.dart` convert to the company's IANA zone (Dart's core `DateTime` has no zone-conversion of its own) — locale-aware month names were deliberately not added on mobile, since that would need the much larger `intl` package's CLDR data for a phase whose mobile scope is a read-only profile view. UI evidence (screenshots under `docs/evidence/phase-3.3/`, local-only, captured via a real signed-in browser session against a locally-running API + admin dev server, not a stub): the Company Settings page in both themes showing the real seeded Acme Energy tenant's profile, branding, and overview sections; the branded 403 for `field_inspector`. A real browser render caught a genuine bug no unit test had: Chrome's `Intl.supportedValuesOf("timeZone")` does not include `"UTC"`, so a fresh tenant's Timezone `<select>` silently displayed the alphabetically-first zone instead of the bound `"UTC"` value — fixed by unioning `"UTC"` into the option list explicitly. Real-Firestore/Storage proof against `thinking-case-469504-c0` (scripted directly against the running API and Identity Toolkit REST endpoints): signed in as the seeded `company_admin`, fetched the real profile, updated industry/timezone/locale/contact fields and confirmed the exact before/after audit entry, explicitly cleared those same optional fields back to `null` and confirmed they returned to their original state. The real logo upload initially failed — the project's Storage bucket had never been provisioned (both `.appspot.com` and `.firebasestorage.app` candidate names returned "bucket does not exist" from the GCS API); the user enabled Storage in the Firebase console, after which the identical proof succeeded: uploaded a real 74-byte PNG, received a genuine V4 signed URL, confirmed that exact URL served the exact PNG bytes back over HTTPS, then removed it and confirmed the reference cleared. Confirmed 403 for `field_inspector` against the same live API. The tenant was left exactly as found (industry/contact fields back to `null`, timezone/locale back to `UTC`/`en-US`, no logo). GitHub Actions run 29919254634 passed all four jobs (API 32s, Admin 1m26s, Mobile 1m37s, Contracts 2m21s). PR: https://github.com/umerali06/EnergyVerse/pull/21. The audit viewer (3.4) and Super-Admin cross-tenant management (3.5) remain deferred. | Passed | 2026-07-22 |
| 3.4 — audit log viewer | Backend: Ruff and strict mypy (63 source files) passed; pytest reported 156 passed with 3 credential-only skips, including 15 new tests in `test_audit.py` covering tenant isolation (a filtered query for another tenant's action returns empty even when that action exists), 403 without `audit.read`, unverified-email 403, date-range narrowing (an in-range action returns its events, an out-of-range one returns none for the same window), actor/action/target-type filters individually and combined, free-text search matching stringified metadata and missing on a nonexistent token, cursor pagination across 25 events in 4 pages with no overlap/duplication, an invalid cursor's exact `invalid_cursor` 422, a >730-day range's exact `date_range_too_wide` 422, deleted/unknown-actor enrichment resolving to `null` name/role without crashing, facets returning the real distinct actions/target-types seen in range, a monkeypatched low cap proving the `truncated` flag on both the list endpoint and export's 413, an exported CSV's exact 8-column header and row count for a filtered set, and a `GET` against all three audit routes leaving the `audit_logs` document count unchanged (the read-your-own-reads-aren't-audited policy). `audit.read` was added to the Phase 0.4 catalog and granted to `hse_manager`/`executive` (verified via the existing `test_seed_and_rbac.py` role-matrix mirror, updated for the new key and role grants); no new Firestore index was needed — `list_range` reuses D-019's existing `company_id + created_at` composite index as both floor and ceiling, confirmed by extending the fake Firestore query stub to support `<=` alongside `==`/`>=`. OpenAPI export and the pinned TypeScript Fetch / Dart Dio clients were regenerated for `AuditApi`/`AuditLogEntry`/`AuditLogPage`/`AuditLogFacets`; the contracts drift check passed clean, including a new 413 response code added to the shared `error_responses` helper and a documented JSON-schema exception for the one CSV-returning operation. Admin: 122 Vitest tests passed (11 new in `audit-page.test.tsx` covering loading/empty/error states, the branded 403 for a role without `audit.read`, an action-filter change re-fetching with the right query param, an expanded row rendering the before/after diff, a truncated-range notice, export-disabled-when-empty, a real export triggering `createObjectURL`/download, and the reduced-motion render path) plus a new `FilterChip` design-system primitive (the first dismissible-chip component in this codebase) and a `formatCompanyDateTime` addition to `dashboard/format.ts`; ESLint clean; production build compiled all 25 routes (`/audit` at 5.83 kB); bundle budget held at 342.2 KB shared (unchanged). A genuine gap was caught before it shipped: the initial hook design called the `company.settings`-gated `getCompany()` for timezone/locale, which would 403-toast for `hse_manager`/`executive` viewers who hold `audit.read` but not `company.settings` — fixed by sourcing `companyTimezone`/`companyLocale` from the already-available `CurrentUser` (the same fields 3.3 added to `/me`) instead. Mobile: `flutter analyze` reported no issues and `flutter test` reported all 86 tests passed (6 new in `audit_screen_test.dart` covering loading/empty/error, cursor pagination, the before/after detail sheet, and the route-level 403 for a role without `audit.read`), plus the `ApiContract` fakes updated across eight existing test files for the two new read-only methods; `built_collection`/`built_value` were promoted from a dev-only/transitive dependency to explicit direct dependencies since app code (not just tests) now needs them to read `AuditLogEntry.metadata`. Export is intentionally admin-web-only on mobile, extending the D-023/D-026 precedent. GitHub Actions run: pending (not yet pushed this session). PR: pending. **Deferred this session** (time-boxed): real-Firestore/browser evidence (screenshots in both themes, a live filtered query, an expanded real entry, the 403 screen, and a real exported CSV opened and verified) and running `reconcile_roles.py` against the live `thinking-case-469504-c0` project to backfill `audit.read` onto real `hse_manager`/`executive` assignments — all CI-safe automated checks (backend/admin/mobile suites, lint, mypy, contracts drift, production build, bundle budget) are green and reported above; the real-project walkthrough should be completed as a follow-up before this phase is considered fully closed out. Super-Admin cross-tenant management (3.5) remains deferred. | Passed (CI-safe); real-creds evidence deferred | 2026-07-22 |
| 3.4 follow-up — `reconcile_roles.py` backfill | Closed the one open 3.4 gap as a 3.5 prerequisite: listed all real `companies` documents in `thinking-case-469504-c0` and confirmed exactly one already-registered non-demo tenant exists, `cmp_feee017b83914cdd8323745e6359cc32` (the Phase 1.2 real signup, "FEV Signup Verification Test"). Ran `poetry run python -m scripts.reconcile_roles --company-id cmp_feee017b83914cdd8323745e6359cc32`, which reconciled all seven system roles idempotently. Verified directly against Firestore: `hse_manager` and `executive` now hold `audit.read` on that tenant; `field_inspector` correctly does not. Full real-Firestore/browser evidence for the audit viewer itself (screenshots, a live filtered query, a real exported CSV) remains a separate open item, not required to unblock 3.5. | Passed | 2026-07-22 |
| 3.5 — Super-Admin cross-tenant platform administration | Backend: Ruff and strict mypy (63 source files) passed; pytest reported 167 passed with 3 credential-only skips, including 11 new tests in `test_platform.py` covering a table-driven permission matrix across all 5 `/platform/*` routes for every one of the 7 system roles (only `super_admin` passes, all others 403 `forbidden`), a cross-tenant list returning both real seeded tenants with correct real per-tenant user counts, a 404 `company_not_found` for an unknown id, the explicit reverse-guard test (`test_super_admin_home_company_route_stays_tenant_scoped` — a super-admin's `GET /api/v1/company`/`GET /api/v1/users` still resolve only their own home tenant, cross-checked against a direct repository read of the other tenant's real user ids to confirm none leak through), a 422 for an out-of-enum status value, a full suspend→409-on-repeat→reactivate round trip, `test_suspended_company_blocks_login_and_me` (suspends a tenant via the service directly, then drives the REAL `get_current_user` dependency chain — not an override — to confirm `GET /api/v1/auth/me` 403s with `company_suspended` and 200s again after reactivation), subscription-tier persistence reflected back through 3.3's `GET /api/v1/company`, a 422 for an unrecognized tier, and dual audit-write assertions (one entry in the target tenant's own `audit_logs` with `metadata.cross_tenant=true`/`metadata.acting_company_id`, one under the reserved `CompanyScope(company_id="__platform__")` with `action="platform.company.suspended"`), plus real-count assertions on `GET /platform/stats`. New `app/admin/` package (`AdminScope`, `AdminCompanyService`, `get_admin_scope`), `CompanyRepository.list_all()`, and a `company_suspended` check added to `get_current_user`. OpenAPI export and the pinned TypeScript Fetch / Dart Dio clients were regenerated for `PlatformApi`/`PlatformCompanySummary`/`PlatformCompanyPage`/`PlatformCompanyDetail`/`PlatformStats`/`UpdateCompanyStatusRequest`/`UpdatePlatformCompanyRequest`; the contracts drift check and `test_openapi.py`'s operation-id golden list were updated and passed clean. Admin: 135 Vitest tests passed (13 new — 10 in `platform-page.test.tsx` covering loading/empty/error/403 states, real tenant counts and real stats numbers, the suspend confirmation dialog's cancel-leaves-untouched and confirm-flips-status paths, the symmetric reactivate flow, subscription-tier edit/save, and reduced motion; 1 in `design-system.test.tsx` for the new `ConfirmDialog` primitive; 2 in `app-shell.test.tsx` proving the new "Platform" nav group is invisible to a `company_admin` and visible to a `super_admin` holding `platform.admin`) plus a new `PlatformApiClient` type and `apps/admin/src/api/client.ts` wrapper methods; ESLint clean; `tsc --noEmit` clean (one pre-existing, unrelated `users-page.test.tsx` type-narrowing warning predates this phase and was left as found); production build compiled all 27 routes (`/platform` at 4.61 kB); bundle budget held at 342.2 KB shared (unchanged — the new route lazy-loads under `/platform`). Mobile: unaffected by design (platform administration is web-only, extending the D-023/D-026 precedent to a whole module); `flutter analyze` reported no issues and all 86 existing tests still passed. Real-project proof against `thinking-case-469504-c0`: the seed's already-declared `super_admin@acme.example.invalid` demo user had never been provisioned as a real Firebase Auth account for this role — ran `scripts.seed --with-auth-users` (idempotent, only touches declared demo emails) to provision it, then flipped its `email_verified` to `true` via the Admin SDK (a one-time step every other demo user had already received in earlier sessions). Signed in as the real `super_admin`, confirmed `/me` resolves `role_key=super_admin` with `platform.admin` in its permission set, listed all 3 real tenants (`acme-energy`, `beta-utilities`, and the real non-demo signup `cmp_feee017b83914cdd8323745e6359cc32`), viewed the real non-demo tenant's detail, assigned it a real tier (`starter`), suspended it, confirmed `GET /platform/stats` reflected real totals, reactivated it, and confirmed the dual audit entries landed in both that tenant's own `audit_logs` and the `"__platform__"` platform trail. The reverse-guard proof was run live too: the same `super_admin` token's `GET /api/v1/company`/`GET /api/v1/users` calls returned only Acme's own data. Cleanup: the real non-demo tenant's `subscription_tier` couldn't be restored to its exact pre-3.5 legacy value (`"unassigned"`, a placeholder from its original 1.2 signup that predates the new 4-value enum and is rejected by the new endpoint by design) through `/api/v1/platform/companies/{id}` itself, so that one restoration step went directly through `CompanyRepository.update`, matching every prior phase's "leave the tenant exactly as found" convention; its `status` was already fully restored to `active` through the real endpoint. GitHub Actions run 29952457053 passed API (29s), Admin (1m17s), Mobile (1m44s), and Contracts (2m16s). PR: https://github.com/umerali06/EnergyVerse/pull/23. Phase 3 is now **COMPLETE**; Phase 4 scope remains to be defined per `PHASE_TRACKER.md`. | Passed | 2026-07-23 |
| 4.1 — asset data model, facility/area hierarchy, and backend CRUD | Backend: Ruff and strict mypy (80 source files) passed; pytest reported 203 passed with 3 credential-only skips (one unrelated real-Firebase auth-integration test hit a transient gRPC `DeadlineExceeded` on the full-suite run and passed cleanly on an isolated retry — not a regression, nothing in this phase touches auth). 36 new tests across `test_facilities.py`/`test_areas.py`/`test_assets.py` cover tenant isolation on every list/get route, table-driven permission gating across all 7 system roles for both read and write routes, hierarchy integrity (unknown and cross-tenant facility/parent rejected with 404, an area from the wrong facility rejected with 422 `area_not_in_facility`, self-parenting rejected with 422), the category catalog accepting the ten spec values plus `Other` (422 `invalid_category` otherwise, 422 `category_other_required` when `Other` lacks a subtype), `current_status` defaulting to `Healthy`, `GET /assets` filters/search/sort/pagination (including a cursor walk across all 11 seeded assets with no overlap or duplicates, and proving the new fake-Firestore `order_by` genuinely orders), the always-empty correctly-shaped `GET /assets/{id}/history` stub, and soft-delete excluding a row from both list and get while a 409 blocks deleting a facility/area that still has children. `test_seed_and_rbac.py` and `test_openapi.py` were updated for the 4 new permission keys (21-key catalog), the new per-role expected sets, facility/area/asset field-shape and tenant-isolation assertions, and the 16 new operation IDs. New: `TenantRepository._soft_delete()` (this codebase's first soft delete), `AssetRepository.query()` (its first Firestore-level filtered+ordered read, backed by four new composite indexes in `firestore.indexes.json`), and a small `order_by` addition to the fake Firestore test client. OpenAPI export and the pinned TypeScript Fetch/Dart Dio clients were regenerated for `FacilitiesApi`/`AreasApi`/`AssetsApi` and their models; running export+generate a second time produced zero diff against the committed contracts (the actual CI drift check). Real-project proof against `thinking-case-469504-c0`: `scripts.seed` created real `facilities=2`/`areas=4`/`assets=11` for the Acme tenant (permissions grew 17→21 as expected); created a probe area and asset directly through the repository layer, updated the asset's `current_status` to `Warning`, soft-deleted both, and confirmed the exact `area.created`/`asset.created`/`asset.updated`/`asset.deleted`/`area.deleted` audit trail, all correctly `company_id`-scoped to Acme; re-ran the seed to confirm idempotency (identical counts); then removed the two probe documents directly so the real tenant's demo dataset counts stay exactly as the seed produces them, restoring the tenant to its pre-verification state per the established convention. No UI was built this phase, so there is no browser/screenshot evidence to capture. GitHub Actions run: pending (not yet pushed this session). PR: pending. Phase 4 is now **In progress**; asset management UI (4.2), create/edit/photo (4.3), KPI widgets (4.4), and QR scanning (4.5) remain deferred. | Passed | 2026-07-23 |
| 4.2 — asset list + detail UI (admin + mobile) | Backend: zero net changes this phase (only the generated clients' existing Assets/Facilities/Areas surface was wired into the hand-written wrappers) — `poetry run pytest` reported 202 passed, 3 credential-only skips, and 1 environment-flaky failure in `test_real_firebase_password_sign_in_and_me`: this sandboxed dev VM's system clock measured ~32s behind real time (confirmed via a precise Node HTTPS `Date` header comparison), exceeding the Admin SDK's 30-second `clock_skew_seconds` tolerance in `app/auth/verifier.py`; not a regression (that file has no diff) and not reproducible on CI's accurate-clock runner. Admin: the full Vitest run reported 152 passed with 6 credential-only skips, including 17 new tests — 11 in `assets-page.test.tsx` (loading/empty/error states, search/category/status/sort re-fetch args, cursor pagination, the facility→area cascade correctly clearing area on facility change, "Clear all", and the branded 403 for a role without `assets.read`) and 6 in `asset-detail-page.test.tsx` (all overview fields from a real-shaped fixture, the coordinates+"View on map" link and its href, the no-GPS fallback message, sub-asset links calling `listAssets({parentAssetId, limit:100})`, each reserved tab's exact empty copy, and History calling `getAssetHistory` and rendering its empty state). ESLint reported no warnings/errors; the production build compiled all 27 routes including the new `/assets` and the app's first dynamic route, `/assets/[id]` (`ƒ` server-rendered, confirmed in the build output). A real defect was found and fixed while capturing browser evidence: `nav-config.tsx`'s `findNavItem` required an exact route match, so the shell breadcrumb read "FEV / Not found" on the new dynamic detail route; it now reuses the same prefix-match rule `isRouteActive` already applies to sidebar highlighting (the existing `app-shell.test.tsx` 16-test suite stayed green after the fix). Mobile: `flutter analyze` reported no issues and `flutter test` reported all 92 tests passed, including the new `assets_screen_test.dart` (list loading/empty/error/pagination, the facility→area cascade, opening the new pushed-route detail screen with overview + reserved-tab empty states, and the route-level 403 for a role without `assets.read`) plus the `ApiContract` fakes updated across 8 existing test files (`app_shell_test.dart`, `audit_screen_test.dart`, `auth_login_test.dart`, `company_profile_screen_test.dart`, `dashboard_screen_test.dart`, `permission_gate_test.dart`, `roles_screen_test.dart`, `users_screen_test.dart`, `widget_test.dart`) for the 7 new interface methods, plus one pre-existing `app_shell_test.dart` case ("tapping a bottom destination routes to its Coming soon page") retargeted from `/assets` to `/work-orders` since Assets is no longer a Coming-soon module. Real-project proof against `thinking-case-469504-c0`, scripted directly against the running API (script deleted after use, not committed): confirmed the real seeded data resolves end-to-end through the exact 4.2 query shapes — 2 facilities, 4 areas, 11 assets across 5 categories/all 3 statuses with correct facility/area names, the full `AssetDetail` field set (23 keys) on a single-asset fetch, and the expected always-empty, correctly-shaped `GET /assets/{id}/history` page. UI evidence (screenshots under `docs/evidence/phase-4.2/`, local-only): the admin asset list in both themes with a `Facility: Compressor Station 2` filter chip active and "Clear all" visible, real seeded rows (status pills for Healthy/Warning/Critical, facility→area breadcrumbs); the asset detail Overview tab in both themes showing all real fields, the coordinates+map link, and the correctly-fixed "Assets" breadcrumb; and the Inspections tab's honest "No inspections yet" empty state in both themes. **Deferred this session** (time-boxed, same precedent as 3.4): mobile browser/screenshot evidence was not captured — mobile UI correctness for this phase instead rests on the 92 passing widget tests (list, detail, RBAC, theming, pagination, cascade) plus `flutter analyze`. GitHub Actions run 30212684962 passed API (36s), Admin (1m34s), Mobile (1m32s), and Contracts (2m13s). PR: https://github.com/umerali06/EnergyVerse/pull/25. Asset create/edit and photo capture (4.3) is next. | Passed (CI-safe); mobile browser evidence deferred | 2026-07-26 |
| 4.3 — asset create/edit + media upload | Backend: Ruff and strict mypy passed; the focused asset/OpenAPI suite reported 28 passed. Coverage includes required-field and GPS-pair validation, same-tenant facility/area/parent checks, parent-cycle rejection, case-insensitive tenant-scoped asset-tag duplicate 409s, private company/asset/kind Storage paths, MIME/size rejection, structured metadata, atomic array transforms, signed URLs, object/reference deletion, audit writes, tenant isolation, and `assets.write` denial. Admin: the reusable create/edit form, permission-gated controls, and grouped media manager are implemented; the full Vitest suite reported 152 passed with 6 credential-only integration skips, and the Next production build compiled all 27 routes including `/assets/new` and `/assets/[id]/edit`. Mobile: native Android/iOS runners, create/edit UI, camera/gallery/document picking, media render/upload/remove, and write gating are implemented; `flutter analyze lib` reported no issues and all 92 tests passed. OpenAPI and both pinned clients were regenerated twice; the contract diff hash was identical before/after (`20d0e6bfa6342b4b813c628431ee66e9c9724560`), proving deterministic drift-free generation. Live proof against `thinking-case-469504-c0`: created asset `asset_30be6b5e7d004fdd97d4cf946cb8886d`, uploaded a JPEG and PDF under its exact scoped photo/document paths, received working signed URLs, edited a field, removed both media objects, confirmed `asset.created`, two `asset.media_uploaded`, `asset.updated`, and two `asset.media_deleted` audits, then removed the proof asset/object state. Native identity verification found no `com.example.fev_mobile`/`com.example.fevMobile`: Android namespace/applicationId and iOS bundle ID are `com.flacronenterprises.energyverse`, and both display `EnergyVerse`. Environment limitations: Android native compilation cannot run because this Windows host has no Android SDK/`ANDROID_HOME`; iOS compilation requires macOS/Xcode; browser screenshot capture was unavailable because the required in-app browser runtime failed to initialize its kernel assets. Automated theme, reduced-motion, permission, and UI suites remain green; visual screenshots are therefore deferred as environment evidence, not replaced with synthetic captures. GitHub Actions run 30252741780 passed API (33s), Admin (1m35s), Mobile (1m54s), and Contracts (2m21s). Draft PR: https://github.com/umerali06/EnergyVerse/pull/26. | Passed (visual/native host evidence deferred) | 2026-07-27 |
| 4.4 — dashboard KPI widgets + pluggable widget framework (resolves 2.3) | Backend: Ruff and strict mypy (82 source files) passed; `poetry run pytest` reported 215 passed, 3 credential-only skips, 0 failures, including 6 new tests in `test_dashboard_assets.py` covering the real seeded tenant's exact status/total counts (11 total, 8 healthy, 2 warning, 1 critical) and per-facility breakdown, table-driven `assets.read` gating across all 7 system roles, tenant isolation (a second tenant sees all-zero counts, never Acme's), a brand-new zero-asset tenant returning all-zero counts without error, and soft-deleted assets excluded from every count. New `AssetRepository.count()` uses a genuine Firestore `count()` aggregation query (D-039) rather than the D-019 bounded-read-then-count pattern; the test fake (`tests/fakes/firestore.py`) gained aggregation support plus a fix for how Firestore rewrites an equality-to-`None` filter into a distinct unary `IS_NULL` operator (a real gap the new soft-delete-exclusion filter exposed — every existing equality filter in this codebase had only ever compared against non-null values). `test_openapi.py`'s stable-operation-id list was updated for `get_dashboard_assets_summary`. OpenAPI export and the pinned TypeScript Fetch/Dart Dio clients were regenerated; the resulting diff is limited to the new endpoint's route, its 3 new models (`AssetDashboardSummary`/`AssetCategoryCount`/`AssetFacilityCount`), and the generated per-client metadata files — confirmed clean by comparing against the untouched sibling models. Admin: the full Vitest suite reported 166 passed with 6 credential-only skips, including 4 new tests in `widget-registry.test.tsx` (permission-table-driven filtering, tier gating, empty-registry rendering, and a deliberately-throwing test widget isolated by its own error boundary without affecting a healthy sibling) and 5 in `asset-widgets.test.tsx` (real seeded counts render, a different mocked value proving nothing is hardcoded, the condition chart's empty state for a zero-count tenant, and the Critical/Total cards each navigating to the correctly filtered/unfiltered `/assets` route); `dashboard-page.test.tsx` was updated to cover the real asset widgets replacing the old static tile, the roadmap card still showing reserved empty-state tiles for unbuilt modules only, and one asset widget's own error state rendering without breaking the rest of the dashboard; `assets-page.test.tsx` gained a test proving `?status=Critical` in the URL hydrates the Status filter on mount. Mobile: `flutter analyze lib test` reported no issues; `flutter test` reported all 101 tests passed, including a new `widget_registry_test.dart` (4 tests, same coverage as admin's) and new coverage in `dashboard_screen_test.dart` for real asset widget rendering, the field_inspector task-focused subset (Total + Critical only, no condition chart) versus the full set for other roles, Critical-card navigation landing on the pre-filtered assets list, and the condition chart's own error state leaving the rest of the dashboard intact; `assets_screen_test.dart` gained a test proving a `Navigator` push with a status argument arrives pre-filtered; the `ApiContract` fakes across 10 existing test files were extended with `getDashboardAssetsSummary`. No new Firestore composite indexes were needed — every count query uses only equality filters. Per D-040 (new this session, applies to every future phase), no screenshot/browser evidence was attempted; visual/UX correctness is verified by the product owner manually. GitHub Actions run 30343867578 passed API (37s), Contracts (2m15s), Mobile (1m55s), and Admin (1m39s) — the first CI attempt (30342849101) caught a real lint defect (`reserved-widgets.tsx` used `module` as a loop variable, tripping Next.js's `no-assign-module-variable` rule), fixed by renaming it to `reservedModule`. PR: https://github.com/umerali06/EnergyVerse/pull/27. Phase 2.3 is resolved by this implementation; QR code asset scanning (4.5) is next. | Passed | 2026-07-28 |
| 4.5 — QR code generation and scanning (Phase 4 now COMPLETE) | Backend: Ruff and strict mypy (86 source files) passed; `poetry run pytest` reported 225 passed, 3 credential-only skips, 0 failures — 20 new tests: `test_qr.py` (10) covers `qr_code_id` generated + unique on create (never equal to the asset's own id), a successful resolve returning the full scan surface with hard-zeroed `inspections_total`/`maintenance_total`/`work_orders_total`, an unknown code and a cross-tenant code both returning the identical `qr_code_not_found` 404, unauthenticated 401, a role lacking `assets.read` 403, the resolve being audited (`asset.qr_scanned` with the correct actor/target), and the printable label endpoint's URL shape/gating/cross-tenant 404; `test_backfill_qr_codes.py` (2) proves the backfill assigns a code to a simulated pre-4.5 asset, is idempotent on re-run, and never reuses a code already in use. `scripts/seed.py` was updated so every seeded demo asset gets a real `qr_code_id` (previously only reserved as a null field since 4.1). `test_openapi.py`'s operation-id list gained `get_asset_qr_label`/`resolve_qr_code`. New `app/assets/qr.py` (opaque token generation + collision check, deep-link URL builder), `app/api/v1/qr.py` (the `/api/v1/qr/{code}/resolve` router), 3 new `AssetRepository` methods (`get_by_qr_code`, `list_missing_qr_codes`, `backfill_qr_code`, `record_scan`), and a new `Settings.app_base_url`. OpenAPI export and the pinned TypeScript Fetch/Dart Dio clients were regenerated twice; both runs produced byte-identical `openapi.json` (same SHA-256) and an identical changed/added file set — deterministic, drift-clean. Admin: the full Vitest suite reported 171 passed with 6 credential-only skips, including 2 new tests in `asset-detail-page.test.tsx` (the new "QR Code" tab renders the code + Print/Download actions once loaded, and an unavailable state when the label fails to load) and a new `qr-resolve-page.test.tsx` (3 tests: redirect to the asset on a successful resolve, a branded not-found state for a 404, and a generic error state for a network failure); ESLint clean; production build compiled all 28 routes including the new dynamic `/qr/[code]` (2.23 kB). A new `react-qr-code` dependency renders the QR client-side (SVG only, no canvas). Mobile: `flutter analyze` reported no issues; `flutter test` reported all 119 tests passed, including new `qr_scan_controller_test.dart` (8 tests: code extraction from both a bare code and a full deep-link URL, resolve success/404/network-error states, reset, and an empty manual entry being ignored), `qr_scan_screen_test.dart` (7 tests, including 3 for `cameraErrorMessage()`'s permission-denied/unsupported/generic copy), and `qr_scan_result_screen_test.dart` (2 tests, including tapping "Start Inspection" landing on the existing `ComingSoonScreen`); every widget test injects a fake scanner slot so none ever mounts the real `mobile_scanner` camera plugin (live scanning is a physical-device check, per the module's own doc comment); `dashboard_screen_test.dart` gained a test confirming the new "Scan QR code" quick action is offered to a role holding `assets.read`; the `ApiContract` fakes across 9 existing test files were extended with `resolveQrCode`. `CAMERA` permission added to `AndroidManifest.xml`; iOS's existing `NSCameraUsageDescription` (from 4.3's photo capture) was extended to also mention QR scanning. Real-creds proof against `thinking-case-469504-c0` (scripted directly against the live Firestore project via the service layer, script deleted after use): ran the backfill and it assigned `qr_code_id` to all 11 pre-existing Acme assets that predated this phase; created a real probe asset and confirmed a `qr_code_id` was auto-generated; resolved it successfully as the same tenant; attempted to resolve the identical code as a Beta-Utilities scope and confirmed the exact `qr_code_not_found` rejection (proving the cross-tenant 404, not just a unit-test double); confirmed the printable-label endpoint's `url` matched `{APP_BASE_URL}/qr/{code}`; then hard-deleted the probe asset, restoring Acme's real dataset to its pre-verification state. Live camera scanning itself was not exercised (no physical device in this session) — the scan/manual-entry/error-state logic is fully covered by the widget tests above, per D-040's evidence policy and the phase brief's own "camera scanning cannot run in CI" carve-out. GitHub Actions run 30396267759 passed API (39s), Admin (1m37s), Contracts (2m27s), and Mobile (2m3s). PR: https://github.com/umerali06/EnergyVerse/pull/28. Phase 4 is now **COMPLETE**; Phase 5 scope remains to be defined per `PHASE_TRACKER.md`. | Passed (CI-safe; live camera scan is a manual physical-device check) | 2026-07-29 |
| 7.1 — inspection data model, backend CRUD, and lifecycle (flagship foundation) | Backend: Ruff and strict mypy (92 source files) passed; `poetry run pytest` reported 266 passed, 3 credential-only skips, 0 failures — 37 new tests: `test_inspections.py` (27) covers tenant isolation, cross-tenant 404, RBAC table-driven across all 7 system roles for read/write, create-idempotency (identical resubmit is a true no-op at the same revision; a conflicting resubmit 409s; a malformed UUID 422s), update (real-change revision bump, no-op leaves revision unchanged, a stale `expected_revision` 409s, `PATCH` on a completed inspection 409 `inspection_locked`), checklist-template assignment (success snapshotting 4 items, category-mismatch 422, unknown-template 404), checklist-response validation (unknown/duplicate `item_id` and type-mismatched value all 422 `checklist_response_invalid`), the full lifecycle (start → complete blocked with `missing_item_ids` until answered → complete succeeds; complete with zero template assigned succeeds cleanly; start-twice and cancel-after-complete both 409 `invalid_transition`; cancel-from-draft succeeds), soft-delete exclusion, and asset/status list filters; `test_checklist_templates.py` (10) covers RBAC across the new grant matrix, tenant isolation, cross-tenant 404, create/update/soft-delete with `version` incrementing by exactly 1 per accepted edit, category filtering, and item validation (unknown category, `select` without `options`, non-`select` with `options`, duplicate item ids). `test_assets.py` gained 2 new tests replacing the old always-empty history assertion: a real asset with no inspections still gets an honest empty page, a real completed inspection now appears in `GET /assets/{id}/history` (resolving D-033), and an `in_progress` inspection is correctly excluded. `test_seed_and_rbac.py`-equivalent assertions were extended for the new `checklist_templates`/`inspections` permission group (23 total permissions), seed counts (3 templates, 3 inspections), and tenant-isolation coverage for both new collections; `test_openapi.py` gained the 14 new operation ids. New `app/inspections/` and `app/checklists/` packages (thin-route/fat-service, mirroring `app/assets/` exactly), `app/db/repositories/inspections.py`/`checklist_templates.py`, and `app/api/v1/inspections.py`/`checklist_templates.py`. `AssetManagementService` gained an `InspectionRepository` dependency so `get_asset_history` resolves real completed-inspection rows instead of a hard-coded empty page (D-033). 4 new Firestore composite indexes for `inspections` (mirroring the 4 `assets` indexes) were added to `firestore.indexes.json` **and deployed** to the real project (`firebase deploy --only firestore:indexes --project thinking-case-469504-c0`) — the real-creds run below initially failed with `FAILED_PRECONDITION` until the deploy completed and the index finished building. `scripts/seed.py` gained 3 deterministic-`uuid5` checklist templates (Generic/Pump/Tank) and 3 demo inspections walked through their real lifecycle (one completed with full responses, one in-progress with partial responses, one template-less draft) for the Acme tenant. OpenAPI export and both pinned clients were regenerated twice with an identical file set both times — deterministic, drift-clean. Admin: the full Vitest suite reported 193 passed with 6 credential-only skips (2 integration-test files' `field_inspector` permission-set fixtures updated for the new `checklist_templates.read` grant, though those files stay skipped without real credentials), including 21 new tests across `inspections-page.test.tsx` (6: real data, empty state, retry-capable error, status-filter re-fetch, row navigation, branded 403), `inspection-detail-page.test.tsx` (4: summary + checklist snapshot rendering, Cancel/Delete hidden for a read-only role, Cancel confirms and calls the API, Delete confirms and navigates back to the list), `checklist-templates-page.test.tsx` (7: real data, Create-button visibility and row-navigation gating by write permission, empty state, category-filter re-fetch, branded 403), and `checklist-template-form-page.test.tsx` (4: create with one checklist item, a `select` item with no options rejected client-side, an existing template's fields prefill correctly, saving an edit calls `updateChecklistTemplate`); `asset-detail-page.test.tsx` gained 1 new test confirming the Inspections tab renders real rows linking to `/inspections/{id}` (its pre-existing empty-state test now passes a working `listInspections` fixture instead of leaving the method unmocked); `auth-context.tsx` gained `InspectionsApiClient`/`ChecklistTemplatesApiClient` `Pick<>` types; ESLint clean; production build compiled all 32 routes including the new `/inspections`, `/inspections/[id]`, `/checklist-templates`, `/checklist-templates/new`, and `/checklist-templates/[id]`. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 125 tests passed, including new `inspections_screen_test.dart` (4: real data, empty state, status-filter re-fetch, branded 403) and `inspection_detail_screen_test.dart` (1: navigating from the list renders the full lifecycle/checklist-snapshot detail); `qr_scan_result_screen_test.dart` was rewritten from 2 tests to 3 (real asset info still renders; "Start Inspection" creates a real draft via a fake `createInspection` and lands on the real detail screen; a failed create surfaces a snackbar and stays put) — `QrScanResultScreen` gained an injectable `api` constructor param (mirroring `qr_scan_screen.dart`'s injectable `scannerBuilder` precedent) purely as a testing seam. `ApiContract` gained `getInspections`/`getInspection`/`createInspection` as real (fakeable) interface methods rather than an untestable `AssetWriteContract`-style runtime-cast extension, since the "Start Inspection" widget test needed to fake the write call directly; this required stubbing the three methods across all 11 pre-existing hand-rolled `FakeApi`/`FakeQrApi` test doubles to keep compiling. New `uuid` pub dependency for client-generated inspection ids. A genuine bug was caught by the widget tests, not by inspection: `InspectionDetailScreen` originally called `AuthProvider.of(context)` from `initState()`, which Flutter forbids (inherited-widget lookups must wait for `didChangeDependencies`) — fixed by moving the load trigger there, matching `AssetDetailScreen`'s own established pattern. Real-creds proof against `thinking-case-469504-c0` (scripted directly against the live Firestore project via the service layer, script deleted after use): ran the idempotent seed (picked up the 3 templates/3 inspections for real); created a real checklist template; created a real inspection and confirmed an identical resubmit was a true no-op at the same revision; confirmed it appeared in the asset's inspection list (`list_inspections` filtered by `asset_id`); assigned the new template and confirmed the snapshot; answered the checklist, started, and completed the inspection; confirmed the completed inspection appeared in the real asset's `GET /assets/{id}/history` (D-033, now real); then hard-deleted both probe documents, restoring the tenant's seeded counts exactly. Two earlier verification attempts left orphaned probe documents when the newly-deployed composite index wasn't ready yet (`FAILED_PRECONDITION`) — both were identified and hard-deleted before the final clean run. A manual testing guide for the human was written to `docs/manual-testing/phase-7.1-inspections.md`. GitHub Actions run 30444857921 passed API (38s), Admin (1m49s), Contracts (2m12s), and Mobile (1m53s). PR: https://github.com/umerali06/EnergyVerse/pull/29. Phase 7.1 is now **Done**; the rest of Phase 7 (offline sync, capture, checklist-filling UI, readings, signature, AR, AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-07-29 |
| 7.2 — offline engine: local store, sync queue, and conflict resolution | Backend (small companion fix): Ruff and strict mypy passed; `poetry run pytest` reported 268 passed, 3 credential-only skips, 0 failures — 2 new tests in `test_inspections.py` covering the new `assign_checklist_template` `expected_revision` guard (a correct value succeeds and bumps the revision; a stale value 409s `revision_conflict`, mirroring `update`'s existing pattern exactly). `AssignChecklistTemplateRequest` gained the optional field; `InspectionRepository.assign_checklist_template` and the service layer thread it through the same `RevisionConflictError` path `update` already used. Mobile (the bulk of this phase): `flutter analyze` reported no issues; `flutter test` reported all 146 tests passed — 125 pre-existing plus 21 new: 11 in `local_inspections_repository_test.dart` (createDraft writes a `local_only` row + queues a `create`; `updateInspection` coalesces repeated edits into one not-yet-attempted outbox row but appends a fresh one once the coalesced row has been attempted; `completeInspection` rejects locally with `ChecklistIncompleteError` when a required item is unanswered, with no outbox row created, and succeeds once answered; both `resolveConflict` branches — keep-mine requeues an `update` against the conflict snapshot's revision, use-server's adopts the snapshot and drops every queued mutation; `discardOutboxItem` marks the inspection `error` when it was the last queued row; `retryOutboxItem` clears backoff; `reconcileSessionOwner` preserves data for the same uid and wipes it for a different one) and 10 in `sync_engine_test.dart` (create-offline-then-sync; multi-edit-coalesced-sync dispatching exactly one `updateInspection` call with the final merged field values; the full create→assign→complete lifecycle replaying in order; a transient `network_error` retried via manual "sync now" succeeds without a duplicate outbox row, and correctly stops draining the rest of that pass rather than touching a second inspection's row; a genuine `revision_conflict` — where the re-fetched server state does not match what the queued update wanted — surfaces as `conflict` with a snapshot and drops the outbox row; the "already applied" case — where the server's current state already matches the queued update exactly, meaning an earlier attempt landed before the app died mid-request — is correctly treated as success, not a conflict; a permanent (non-conflict, non-network) error pauses the row so a bare "sync now" does not re-attempt it, but an explicit per-item retry does; two concurrent `kick()`s only ever run one drain at a time (proven via an in-flight counter never exceeding 1); and a pending outbox row survives a simulated app-kill-and-relaunch, using a real on-disk Drift file across two separate `AppDatabase` instances, then drains cleanly on the next "launch"). New `apps/mobile/lib/db/` (`LocalInspections`/`Outbox` Drift tables, `AppDatabase`), `lib/inspections/local_inspections_repository.dart` (the offline-first facade every inspection read/write path now goes through), and `lib/sync/sync_engine.dart` (`SyncEngine`/`SyncProvider`); `ApiContract` gained `updateInspection`/`startInspection`/`completeInspection`/`cancelInspection`/`assignChecklistTemplate` as real interface methods (the established 7.1 precedent for any inspection write path that needs to be fake-able), requiring all 13 existing hand-rolled `FakeApi`/`FakeQrApi` test doubles to gain stubs for the five new methods; `InspectionsController`, `InspectionDetailScreen`, and `QrScanResultScreen` migrated off `ApiContract` onto the repository (every other screen is unaffected); the app shell gained an offline/pending-count `StatusPill`, inspection rows/detail gained a tappable sync-state badge, a two-button conflict-resolution sheet, and a new `SyncQueueScreen` (`/inspections/sync-queue`) with per-item retry/discard. `FevApp` gained an injectable `database` param so every widget test constructs `AppDatabase(NativeDatabase.memory())` explicitly. A genuine production bug was caught by the sync engine test suite, not by inspection: the "already applied" check compared `UpdateInspectionRequest.checklistResponses` (always populated, defaulting to an empty list) against the server's `InspectionDetail.checklistResponses` (nullable, `null` when never set) without normalizing the two — a real replayed update matching in every other way was being misclassified as a genuine conflict purely from this null-vs-empty-list asymmetry; fixed by normalizing both sides to an empty list before comparing. A second, environment-level issue was found and fixed along the way: `flutter_test`'s fake-clock test binding flagged "a Timer is still pending" for nearly every widget test once the app shell held one live `watchOutbox()` Drift stream open on every route — Drift's stream cancellation defers real cleanup to an internal zero-duration `Timer` that a widget-tree teardown with no further pump never lets fire; fixed by having `SyncEngine` recompute its pending count via a plain `ChangeNotifier` listener on the repository (a one-shot query per notification) instead of holding its own `watchOutbox()` subscription, since a `ChangeNotifier`'s listener list is plain callbacks with no Timer involved — the genuinely reactive list/detail screens keep real watch streams and needed a much narrower, per-test `disposeApp()` teardown helper instead. CI: the mobile job gained a `dart run build_runner build --delete-conflicting-outputs` step before `flutter analyze` (Drift's generated `*.g.dart` files are gitignored, not committed). Real-creds proof against `thinking-case-469504-c0` (scripted directly against the live Firestore project via the service layer, script deleted after use): created a real probe inspection via `create_draft` and confirmed an identical resubmit was a true no-op at revision 1 (the exact contract a retried offline `create` outbox row depends on); confirmed a correct `expected_revision` on `update_inspection` succeeds and a stale one 409s `revision_conflict`; confirmed the same for the newly-guarded `assign_checklist_template` (a correct `expected_revision` succeeds, a stale one 409s); then hard-deleted the probe inspection. Regenerating `packages/contracts/openapi.json` and the downstream Dart/TypeScript clients for the new `expected_revision` field hit a persistent Windows file-lock on `packages/contracts/generated/dart-dio` in this dev environment (survived killing dart/flutter processes, repeated retries, a VS Code reload, and a Node `maxRetries`/`retryDelay` fix to `gen-clients.mjs` for a related but distinct transient-timing issue, committed regardless since it's a real, harmless improvement). Rather than leave that unresolved, the first CI push doubled as the regeneration: the `contracts` job's Linux runner has no such lock and correctly regenerated everything from source, failing its own drift check exactly as designed (a genuine, expected diff, not a bug in the check); that job's full computed diff (7 files: the new model/doc/test/`.g.dart` across both clients, `openapi.json`, and an unrelated transitive `pubspec.lock` bump from time passing since the last regeneration) was extracted from the run log and applied directly to the repo, and `LocalInspectionsRepository.assignChecklistTemplate` now threads `expectedRevision` through like every other mutation type. A manual testing guide (airplane-mode walkthrough) was written to `docs/manual-testing/phase-7.2-offline-engine.md`. GitHub Actions run 30480723762 passed API (41s), Admin (1m43s), Contracts (2m31s), and Mobile (2m53s) — the first push's run (30477489348) caught the expected `Contracts` drift failure described above before it was resolved. PR: https://github.com/umerali06/EnergyVerse/pull/30. Phase 7.2 is now **Done**; the rest of Phase 7 (capture, checklist-filling UI, readings, signature, AR, AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-07-29 |
| 7.3 — inspection start flow and checklist filling | Backend: Ruff and strict mypy (92 source files) passed; `poetry run pytest` reported 269 passed, 3 credential-only skips, 0 failures — 1 new test in `test_inspections.py` (`test_checklist_response_partial_update_preserves_other_answers`) proving three sequential PATCHes, each answering a different single item, leave all three answers present and correctly upsert-in-place on a re-answer rather than duplicating. `InspectionService.update_inspection` now merges validated incoming responses into the current stored array by `item_id` (a new `_merge_checklist_responses` static helper) instead of replacing the whole array — this was a real bug found while building this phase's autosave, not a designed change (see D-049): harmless under 7.1/7.2 since nothing had ever called `update_inspection` with a checklist subset, but exactly what 7.3's per-item autosave does on every answer. No route/schema change, so no contracts regeneration was needed (confirmed via `git diff --exit-code -- packages/contracts`, clean). Mobile: `flutter analyze` reported one issue found and fixed (`prefer_initializing_formals` in a test fixture) then a clean re-run; `flutter test` reported all 154 tests passed (up from 146, net of some pre-existing tests renamed/restructured along the way) — new/updated coverage: `local_inspections_repository_test.dart` gained 6 tests (`updateInspection` upserts by `itemId` instead of replacing the array across two sequential single-item calls; re-answering an item updates it in place; `refreshChecklistTemplatesFromNetwork` populates the new local cache from a fake API; `selectChecklistTemplateForCategory` picks the most-recently-updated match when two templates share a category, falls back to `Generic` when no category match exists, and returns `null` when nothing is cached); `inspection_detail_screen_test.dart` was rewritten from 1 test to 3 (a `completed` inspection still renders its checklist fully read-only with no Pass/Fail inputs or Complete button; an `in_progress` inspection renders interactive Pass/Fail for its unanswered required item with Complete disabled, tapping Pass persists the answer to the local database and enables Complete, tapping Complete transitions it to `completed`; opening a locally-seeded stale `draft` with a matching cached template — API fully offline — auto-assigns the template and transitions to `in_progress`, entirely from the local cache); `qr_scan_result_screen_test.dart` gained an assertion that "Start Inspection" now also persists the asset's category locally. New `apps/mobile/test/flutter_test_config.dart` swaps in a `GeolocatorPlatform` double (`isLocationServiceEnabled` resolving to `false` immediately) for the whole suite, since an unmocked platform channel under `flutter_test` can hang rather than throw — this is what `captureCurrentPosition`'s own outer timeout also guards against in production. Two real bugs were found and fixed while building the first test that exercised the merge round-trip end-to-end (not by inspection): (1) the built_value `AnyOf` wrapper's custom serializer round-trips through a fixed 3-type parameter list on decode but rebuilds a mismatched shorter list on re-encode, throwing `RangeError` the first time a decoded-then-re-merged response was re-serialized — fixed by normalizing every response back to its canonical single-type shape immediately before encoding (`_normalizeResponseValue`), applied at every encode call site (local storage and both outbox-payload builders); (2) `_upsertFromServer` stored `detail.status.name`/`detail.inspectionType.name` — built_value's Dart-identifier enum name (`inProgress`) — instead of the wire value (`in_progress`) every local write path and this phase's new status-string gating use, invisible until now because `draft`/`completed`/`cancelled`/`ad_hoc` happen to be spelled identically either way; fixed with a new `dartEnumNameToWire` helper (see D-049 for both). New `LocalChecklistTemplates` Drift table and a local-only `LocalInspections.assetCategory` column (schema v1→v2, `MigrationStrategy.onUpgrade`, this repository's first migration); new `getChecklistTemplates`/`getChecklistTemplate` on `ApiContract`/`ApiService` (the underlying generated `ChecklistTemplatesApi` client methods already existed since 7.1, just unwired), requiring all 14 hand-rolled `FakeApi`/`FakeSyncApi`/`FakeQrApi` test doubles across the suite to gain stubs; new `geolocator` dependency plus Android/iOS location permission entries; new "Start Inspection" button on the asset detail screen. Admin: ESLint reported no warnings/errors and the full `vitest run` reported 193 passed, 6 credential-only skips, 0 failures; the checklist section now shows the assigned template's name/version, each item's type, and a "Filled in the field — read-only here" note. Two real gaps in `inspection-detail-page.test.tsx` were caught by CI, not by inspection, when this session ran the admin suite for the first time this phase (a prior session's TESTING.md draft had only run `next lint`/`tsc --noEmit` on the touched file, never the test suite itself): (1) `InspectionDetailPage`'s new best-effort `apiClient.getChecklistTemplate(templateId)` call had no matching stub on the test file's hand-rolled fake `apiClient`, throwing an uncaught `TypeError: apiClient.getChecklistTemplate is not a function` from the `useEffect` on every test — fixed by adding a `getChecklistTemplate` stub (mirroring the established fake-API-double pattern) plus a new assertion that the template name/version actually renders; (2) the pre-existing "renders the inspection summary and checklist snapshot" test's custom exact-`textContent` matcher for "Vibration normal (required)" broke once this phase's new per-item type badge ("boolean") was added as a sibling text node inside the same element — fixed by updating the matcher to the new full textContent and adding a direct assertion that the type badge renders. CI also surfaced one unrelated pre-existing flake in `dashboard-page.test.tsx` (untouched by this phase): two of its assertions did an unscoped `screen.getByText("1")`, which intermittently collided with a Recharts-rendered `"1"` tick label elsewhere on the page — fixed by scoping both to the Critical Assets stat card's own section via `within(...)`, the same `.closest("section")` pattern the file's own click test already used. Real-creds proof against `thinking-case-469504-c0` (scripted directly against the live Firestore project via the service layer, script deleted after use): created a real probe inspection on the seeded Feed Pump asset, assigned the real seeded Pump checklist template, started it, PATCHed `vibration_normal` alone (confirmed exactly that one response present), PATCHed `bearing_temp_f` alone (confirmed **both** responses present — the merge fix holding against real Firestore, not just the fake test double), confirmed `complete` still correctly 422s `checklist_incomplete` with `seal_condition` unanswered, answered it, completed successfully with all 3 responses intact, then hard-deleted the probe inspection. The mobile-side auto-selection/GPS/interactive-fill behavior is covered by the widget test suite above; verifying it live requires a running device/simulator, which is documented as a manual follow-up in the guide below rather than faked here. GitHub Actions run 30694518466 passed API (37s), Admin (1m51s), Contracts (2m24s), and Mobile (2m53s) — two earlier pushes in this session (30693076053, 30693864600) caught the real admin gaps and the flaky dashboard query described above before they were resolved. PR: https://github.com/umerali06/EnergyVerse/pull/31. Phase 7.3 is now **Done**; the rest of Phase 7 (camera/photo/annotation/voice/readings/signature/AR/AI capture, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-07-30 |
| 7.4 — camera capture (photos + videos), GPS/timestamp, before/after | Backend: Ruff and strict mypy (87 source files) passed; `poetry run pytest` reported 280 passed, 3 credential-only skips, 0 failures — 11 new tests in `test_inspections.py` covering `attach`/`update`/`detach` media: successful attach registers a real `InspectionMedia` and returns a signed URL; an identical-payload replay by `local_id` is a true no-op (no duplicate); a conflicting replay (same `local_id`, different fields) 409s `media_reference_conflict`; attaching before the file actually exists in Storage 422s `media_not_uploaded`; a wrong content type for the declared `kind` 422s `media_content_type_invalid`; an oversized blob 413s `media_too_large`; cross-tenant attach 404s like every other inspection route; `update_media` sets `checklist_item_id`/`before_after_tag` and is idempotent (a no-op) on an already-missing `media_id` rather than 404ing, since the mobile outbox replays at-least-once; `detach_media` removes the reference and a replayed detach on an already-detached id is the identical no-op. New `InspectionMedia`/`InspectionMediaResponse`/`AttachInspectionMediaRequest`/`UpdateInspectionMediaRequest` models (mirroring `AssetMedia`'s shape plus GPS/`captured_at`/`checklist_item_id`/`before_after_tag`); new `InspectionMediaStorage` (alongside `AssetMediaStorage`) that — unlike its asset sibling — never uploads bytes itself, only `verify_uploaded()`s what the mobile client already wrote directly to Storage and issues signed URLs; three new routes on the existing `inspections` router (`POST`/`PATCH`/`DELETE .../media[/{id}]`), all gated by the existing `inspections.write`, all idempotent by `local_id`/`media_id`, and all deliberately carrying no `expected_revision` (D-051) so heavy media traffic can never collide with the checklist-autosave revision protocol. `InspectionRepository` gained `append_media`/`remove_media` (Firestore `ArrayUnion`/`ArrayRemove`, mirroring `AssetRepository`'s) and `update_media` (read-modify-write by `id`, since a partial field update by index isn't expressible as a single array op). New `storage.rules` carve-out — the first-ever exception to "everything is server-mediated" (3.3's `CompanyLogoStorage` doc comment) — allows a client `create`-only write to `companies/{company_id}/inspections/{inspection_id}/media/{file}` gated by the caller's `company_id` custom claim and a 500MB size cap; `allow read: if false` keeps every read signed-URL-only via the backend. Contracts regenerated for the 3 new models and 3 new `InspectionsApi` operations on both generated clients, confirmed drift-clean on a second regeneration. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 180 tests passed (up from 154) across 3 new test files and updates to 2 existing ones. New `local_media_repository_test.dart` (9 tests): `inspectionMediaStoragePath()` mirrors the backend's `object_path()` formula field-for-field; `enqueueCapture` inserts into `MediaQueue`, never `Outbox`; `watchMediaForInspection` scopes correctly; `removeBeforeSync` deletes the local file + row without touching the outbox; before/after tag and checklist-item-id setters; `dueForUpload` respects backoff and the `pausedSentinel` convention (including under `bypassBackoff`); `markReferenced` deletes the now-redundant row; a queued capture survives an app-restart (real on-disk Drift file across two `AppDatabase` instances). New `media_upload_worker_test.dart` (5 tests, against a `FakeMediaUploader`/`FakeMediaUpload` double — not a hand-rolled fake `FirebaseStoragePlatform`, since the concrete `Reference`/`UploadTask` types have private constructors deep in the plugin's app-facing wrapper): a successful upload marks `uploaded` and enqueues `attach_media` onto the *inspection* outbox; progress events update `uploadedBytes` before completion; a retryable Storage error (`retry-limit-exceeded`) backs off instead of pausing; a non-retryable error (`unauthorized`) pauses for manual retry; `removeBeforeSync` cancels an actively in-flight upload. New `media_capture_screen_test.dart` (6 tests): `cameraCaptureErrorMessage()`'s three branches (permission denied, no camera, generic) as pure-function unit tests, mirroring 4.5's `cameraErrorMessage()` precedent; widget tests via an injected `captureBuilder` (mirroring `QrScanScreen`'s `scannerBuilder` seam) prove the capture slot + gallery-fallback buttons render, a simulated capture pops the screen with a `MediaCaptureResult`, and an injected capture error renders. `sync_engine_test.dart` gained a `group('media reference sync (Phase 7.4)')` (4 tests): `attach_media` replays and calls `markReferenced`, clearing the now-redundant `MediaQueue` row; `edit_media`/`detach_media` dispatch their `media_id` + wrapper payload correctly; an inspection can complete while a sibling media item is still locally queued, with `pendingCountForInspection` staying honest about it. `inspection_detail_screen_test.dart` gained 3 tests for the new `InspectionMediaSection`: an honest "No media yet" empty state; a synced media item rendering the "N of M uploaded" count; a locally-queued item rendering its `Queued` upload-state badge. Two of these tests also caught the ListView-virtualization gotcha the file's own prior comment warned about — the pre-existing "answering the required item enables Complete" test needed `ensureVisible` (not just `scrollUntilVisible`) before both taps once the new MEDIA section widened the gap between the checklist row and the Complete button. New `apps/mobile/lib/media/` package: `local_media_repository.dart` (the `MediaQueue` facade, `ChangeNotifier`-based like `LocalInspectionsRepository`), `media_uploader.dart` (the `MediaUpload`/`MediaUploader` seam — `firebase_storage`'s `Reference`/`UploadTask` have private constructors too deep in the plugin's app-facing wrapper to fake directly, so `MediaUploadWorker` depends on this thin interface instead, with `FirebaseMediaUploader` as the real implementation and `FakeMediaUploader` the test double), `media_upload_worker.dart` (the independent drain loop + `MediaProvider`), `media_capture_screen.dart` (capture UI), `inspection_media_gallery.dart` (`InspectionMediaSection` + before/after comparison, wired into `InspectionDetailScreen` between the checklist and the still-reserved voice/readings/signature rows); new `camera`/`firebase_storage`/`video_player`/`video_thumbnail` dependencies; new `RECORD_AUDIO` Android permission and broadened iOS camera/photo-library/location usage descriptions. `ApiContract` gained `attachInspectionMedia`/`updateInspectionMedia`/`detachInspectionMedia` as real interface methods (7.1's established pattern), requiring all 13 hand-rolled `FakeApi`/`FakeSyncApi`/`FakeQrApi` test doubles to gain stubs. **Three real bugs found while building this phase, not designed around** (see D-051 detail and the Phase 7.4 architecture section): (1) `_uploadOne` originally shared one try/catch across the Storage upload *and* the follow-up local-DB reference-enqueue, so a failure in the second incorrectly reverted an already-successful upload back to `failed` — fixed by splitting the steps and having `dueForUpload()` reconsider `uploaded` rows too; (2) `FirebaseMediaUploader` originally resolved `FirebaseStorage.instance` eagerly at construction, throwing `[core/no-app]` without a real `Firebase.initializeApp()` call and breaking every widget test that mounts `FevApp` (not just media ones), since `MediaUploadWorker` — and therefore its default `FirebaseMediaUploader` — is constructed unconditionally in `_FevAppState.initState()`; fixed by resolving it lazily, only when `upload()`/`delete()` actually run; (3) Drift's `DateTimeColumn` round-trips a stored UTC instant back local-flagged (`isUtc: false`, same real instant), which `built_value`'s serializer rejects when building `AttachInspectionMediaRequest.capturedAt` — fixed with a `.toUtc()` re-flag at that one call site. `Image.network` calls in the gallery gained an `errorBuilder` so an unreachable/expired signed URL degrades to a placeholder icon instead of surfacing as an uncaught framework error (caught by a widget test using a fake `https://storage.example.invalid` URL, which `flutter_test`'s network stub always 400s). Admin: ESLint reported no warnings/errors; the full `vitest run` reported 196 passed, 6 credential-only skips (up from 193); `tsc`/`next build` compiled all 29 routes cleanly and the bundle-budget check passed (342.8 KB / 430 KB). `InspectionDetailPage` gained a read-only media grid (thumbnail or "Video" placeholder, before/after tag, GPS, captured timestamp, and the linked checklist item's label looked up from the same `checklistItemsSnapshot` the checklist section already renders) — no upload/edit/remove affordance, matching every other admin capture surface's read-only convention. 3 new tests in `inspection-detail-page.test.tsx`: an honest empty state when no media exists; a synced photo item rendering its before/after tag, GPS, linked checklist item, and a signed-URL `<img>` wrapped in a `target="_blank"` link (scoped via `within(...)` since "Vibration normal" also appears in the checklist section); a video item rendering a "Video" placeholder with no `<img>`. Real-creds proof against `thinking-case-469504-c0` (scripted directly against the live Firestore project + real Storage bucket via the service layer, script deleted after use): created a real probe inspection on the seeded Feed Pump asset, started it, uploaded a real tiny JPEG and a real minimal MP4 directly to Storage at the exact computed path, then called `attach_media` for each — confirming a byte-identical replay by `local_id` is a true no-op, `inspection.media` holds both real references, and the photo's signed URL is genuinely fetchable (fetched bytes matched the uploaded bytes exactly); edited the photo's checklist link + before/after tag; detached the video and confirmed a replayed detach is the identical no-op; hard-deleted both Storage objects and the probe inspection document. (A real bug caught mid-script: the cleanup's Firestore delete call was never `await`ed, silently leaving the first run's probe document live in the real tenant — caught by a follow-up existence check, fixed, and confirmed clean on the corrected re-run.) The mobile-side capture/upload-worker/gallery behavior is covered by the widget/unit test suites above; verifying live camera capture and a real background upload requires a running device, documented as a manual follow-up in the guide below rather than faked here. GitHub Actions run 30740228032 passed API (44s), Admin (1m42s), Contracts (2m10s), and Mobile (2m52s) — first push, no reruns needed. PR: https://github.com/umerali06/EnergyVerse/pull/32. Phase 7.4 is now **Done**; the rest of Phase 7 (annotation, voice, readings, signature, AR, AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-08-02 |
| 7.5 — damage annotation (draw on inspection photos) | Backend: Ruff and strict mypy (89 source files) passed; `poetry run pytest` reported 291 passed, 3 credential-only skips, 0 failures — 11 new tests in `test_inspections.py` covering create/update/delete annotation: successful create registers a real `Annotation` with correct normalized points, `source="manual"`, and a null `confidence`, and never bumps the inspection's `revision`; creating against an unknown `media_local_id` 404s `media_not_found`; an identical-payload replay by `id` is a true no-op; a conflicting replay (same `id`, different `color`) 409s `annotation_conflict`; cross-tenant create 404s like every other inspection route; `update_annotation` edits `damage_type`/`note` while preserving untouched fields (`color`), can move a shape by replacing its `points`, and is idempotent (a no-op) on an already-missing `annotation_id` rather than 404ing; `delete_annotation` removes the entry and a replayed delete on an already-deleted id is the identical no-op; a created annotation survives an unrelated `PATCH /inspections/{id}` (the checklist-autosave path) byte-for-byte, proving the two mutation protocols never collide. New `Annotation`/`AnnotationPoint` entities replace the 7.1-era untyped `list[dict]` placeholder on `Inspection.annotations`; new `AnnotationResponse`/`CreateAnnotationRequest`/`UpdateAnnotationRequest`/`AnnotationPointResponse`/`AnnotationPointInput` wire models. Three new routes on the existing `inspections` router (`POST`/`PATCH`/`DELETE .../annotations[/{id}]`), gated by the existing `inspections.write`, idempotent by client-generated `id`, and deliberately carrying no `expected_revision` (D-055, extending D-051's media precedent) so annotation traffic can never collide with the checklist-autosave revision protocol. `InspectionRepository` gained `append_annotation`/`update_annotation`/`remove_annotation`, mirroring `append_media`/`update_media`/`remove_media`'s `ArrayUnion`/`ArrayRemove`/read-modify-write mechanics field-for-field; `update_annotation` re-validates the merged record through `Annotation.model_validate` (not a bare `model_copy`) so a `points[]` edit is properly coerced back into typed `AnnotationPoint` objects. `test_openapi.py`'s golden operation-id set extended for the 3 new operations. Contracts regenerated for the 5 new models and 3 new `InspectionsApi` operations on both generated clients, confirmed drift-clean. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 193 tests passed (up from 180). New `annotations (Phase 7.5)` group in `local_inspections_repository_test.dart` (7 tests): `createAnnotation` writes to the local `annotations` blob immediately (before any sync attempt) and queues a `create_annotation` mutation; it defaults `source` to manual with a null `confidence`; `updateAnnotation` edits fields in place preserving the rest, can move a shape via `points`, and is a true no-op (no local write, no enqueue) when the `id` isn't found locally; `deleteAnnotation` removes it and queues `delete_annotation`; an annotation drawn fully offline survives a real app restart (on-disk Drift file across two `AppDatabase` instances, mirroring 7.2/7.4's own restart-persistence tests). New `annotation sync (Phase 7.5)` group in `sync_engine_test.dart` (3 tests): `create`/`update`/`delete`-annotation each dispatch through the record outbox (never `MediaQueue`) and clear it on success; `update`/`delete` dispatch the `annotation_id` + request wrapper payload correctly. Building these caught a real, worth-flagging test-fixture gotcha (see the Phase 7.5 architecture section): a fake API stub that echoes back an empty `annotations: []` on `createInspectionAnnotation` silently wipes the just-created annotation from the local cache the moment `SyncEngine` applies that response (`applyMutationSuccess` overwrites the whole row) — not a production bug, since the real backend always echoes full current state, but the fakes needed a matching `_annotationFrom(request)` helper to stay honest. 3 new tests in `inspection_detail_screen_test.dart`: the overlay toggle is absent with zero annotations, present and functional (hides/shows) with at least one, and tapping a photo tile opens `AnnotationCanvasScreen` (asserted via its app-bar title, since the canvas's own image never finishes resolving in a widget test with no real network access — a bounded `pump()`, not `pumpAndSettle()`, is used past that point). New `apps/mobile/lib/annotations/annotation_canvas_screen.dart` (draw/label/undo/redo/select/move/delete, an `AspectRatio`-sized `CustomPaint` surface so normalized coordinates always match the image's own rendered box) and updates to `inspection_media_gallery.dart` (tile-level `AnnotationOverlayPainter` preview + toggle) and `local_inspections_repository.dart` (the `annotations` JSON-blob column, schema v3→v4, and three new `OutboxMutationType` values). A real robustness gap the widget test surfaced and got fixed in production code, not just the test: the canvas's `Image` had no `errorBuilder`, so a broken/expired signed photo URL crashed as an uncaught `NetworkImageLoadException` instead of degrading gracefully like the gallery's own `_networkImage` — fixed with both an `errorBuilder` and an `ImageStreamListener.onError` fallback. `ApiContract` gained `createInspectionAnnotation`/`updateInspectionAnnotation`/`deleteInspectionAnnotation` as real interface methods (7.1's established pattern), requiring all 15 hand-rolled `FakeApi`/`FakeSyncApi`/`FakeQrApi`/`_IdentityApi` test doubles to gain stubs. Admin: ESLint reported no warnings/errors; the full `vitest run` reported 200 passed, 6 credential-only skips (up from 196); `next build` compiled all 29 routes cleanly and the bundle-budget check passed (342.8 KB / 430 KB, unchanged). New `apps/admin/src/inspections/annotation-overlay.tsx` (a read-only SVG overlay, `viewBox="0 0 100 100"` + `preserveAspectRatio="none"` so normalized coordinates map straight onto the tile box, with a per-shape `<title>` tooltip) wired into the existing `InspectionDetailPage` media grid behind a show/hide toggle and a below-tile damage-type summary caption. 4 new tests in `inspection-detail-page.test.tsx`: the toggle is absent with zero annotations on a photo; present, and the SVG shape renders with the correct damage-type text and tooltip, when at least one exists; toggling hides the shape; an annotation belonging to a *different* photo's `media_local_id` never renders on this tile even though the inspection-level toggle still shows (correctly scoped per-photo). One existing test's DOM-scoping query (`.closest("div")`) was replaced with a stable `data-testid="media-section"` after the new toggle button changed the section's nesting — a more robust query than relying on incidental markup shape. A stray literal hex color in the new test fixture tripped the repo-wide "no raw hex colors outside design tokens" ESLint rule (an AST-literal check, correctly blind to the fact that an annotation's `color` field is wire *data*, not a UI style) — worked around by building the string via concatenation (`"#" + "C1123F"`) rather than weakening the rule. Real-creds proof against `thinking-case-469504-c0` (`apps/api/scripts/verify_annotation_roundtrip.py`, kept as a reusable script rather than deleted, mirroring `reconcile_roles.py`'s precedent): created a real probe inspection on the seeded Feed Pump asset, uploaded a real tiny JPEG directly to Storage and attached it, created a real rectangle annotation with `x=0.125/y=0.25` to `x=0.625/y=0.75`, re-fetched the inspection and confirmed the coordinates round-tripped exactly, updated its `damage_type`/`note` while confirming `color` stayed untouched, deleted it, and cleaned up both the Storage blob and the probe inspection — all against the live project, `PASS` end to end. The mobile-side drawing/gesture interaction (freehand paint quality, multi-touch, on-device feel) is covered by the widget tests above for wiring correctness only; verifying it feels good under a finger requires a running device, documented as a manual follow-up in the guide below rather than faked here. GitHub Actions run 31028209643 passed API (36s), Admin (1m50s), Contracts (2m18s), and Mobile (2m59s) — one rerun needed after a `ruff` import-sort fix in the verification script. PR: https://github.com/umerali06/EnergyVerse/pull/33. Phase 7.5 is now **Done**; the rest of Phase 7 (voice, readings, signature, AR, AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-08-05 |
| 7.6 — voice notes (record + attach to inspection) | Backend: Ruff and strict mypy (87 source files) passed; `poetry run pytest` reported 303 passed, 3 credential-only skips, 0 failures — 12 new tests in `test_inspections.py` covering attach/update/detach voice note: successful attach registers a real `VoiceNote` with the Storage-reported size/content-type/duration and never bumps the inspection's `revision`; an identical-payload replay by `local_id` is a true no-op; a conflicting replay (same `local_id`, different filename) 409s `voice_note_reference_conflict`; attaching before the blob is uploaded 422s `voice_note_not_uploaded`; a disallowed content type (e.g. `audio/wav`) 422s `voice_note_content_type_invalid`; an oversized blob (>20MB) 413s `voice_note_too_large`; a `duration_ms` over the 10-minute cap 422s `voice_note_too_long`; cross-tenant attach 404s like every other inspection route; `update_voice_note` sets `checklist_item_id` and is idempotent (a no-op) on an already-missing `voice_note_id` rather than 404ing; `detach_voice_note` removes the entry, never deletes the Storage object, and a replayed detach on an already-detached id is the identical no-op. New `VoiceNote` entity replaces the 7.1-era untyped `list[dict]` placeholder on `Inspection.voice_notes`; new `VoiceNoteResponse`/`AttachVoiceNoteRequest`/`UpdateVoiceNoteRequest` wire models. Three new routes on the existing `inspections` router (`POST`/`PATCH`/`DELETE .../voice-notes[/{id}]`), gated by the existing `inspections.write`, idempotent by `local_id`/`voice_note_id`, and deliberately carrying no `expected_revision` (D-057, extending D-051/D-055's media/annotation precedent). `InspectionMediaStorage` gained a sibling `voice_object_path()` under a `voice/` subfolder, distinct from media's `media/`. `InspectionRepository` gained `append_voice_note`/`update_voice_note`/`remove_voice_note`, mirroring the media repository methods' `ArrayUnion`/`ArrayRemove`/full-array-rewrite mechanics field-for-field. `test_openapi.py`'s golden operation-id set extended for the 3 new operations. Contracts regenerated for the 3 new models and 3 new `InspectionsApi` operations on both generated clients, confirmed drift-clean. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 214 tests passed (up from 193). `MediaQueue.kind` gained an `'audio'` value plus a new nullable `durationMs` column (schema v4→v5, alongside a new `voiceNotes` JSON-blob column on `LocalInspections`); `LocalMediaRepository.enqueueCapture` and `MediaUploadWorker._registerReference` each gained one `kind`-based branch — every other line of the drain loop/backoff/progress/cancel logic is unmodified, shared with 7.4. New `voice_recording_screen_test.dart` (10 tests): `voiceRecordingErrorMessage` names a permission denial specifically and falls back generically otherwise; `formatVoiceNoteDuration` formats `mm:ss` zero-padded; `normalizeVoiceAmplitude` clamps silence to 0, full-scale to 1, maps a mid-range dBFS reading proportionally, and treats NaN/infinite readings as silence rather than crashing; the injectable-builder-driven widget tests (mirroring `media_capture_screen_test.dart`'s precedent) confirm a simulated recording pops the screen with a `VoiceRecordingResult` and a simulated permission denial renders its message without popping. 3 new tests in `local_media_repository_test.dart`: `inspectionVoiceNoteStoragePath` mirrors the backend's `voice_object_path()` formula; `enqueueCapture` with `kind: 'audio'` stores `durationMs` and the `voice/` storage path; a queued voice recording survives a real app restart (on-disk Drift file across two `AppDatabase` instances, mirroring 7.2/7.4's own restart-persistence tests). 1 new test in `media_upload_worker_test.dart` proving an audio item drains through the exact same `syncNow()` path and registers `attach_voice_note` (with `duration_ms`) instead of `attach_media` — the crux of the "reuses the 7.4 worker" claim, exercised structurally rather than duplicating every retry/backoff/cancel variant already covered generically for photo/video. New `voice note reference sync (Phase 7.6)` group in `sync_engine_test.dart` (4 tests): attach/edit/detach-voice-note each dispatch correctly and clear the outbox on success; an inspection can complete while a sibling voice note is still uploading, with `pendingCountForInspection` staying honest. Building the attach test caught the same full-row-overwrite fixture trap flagged in the Phase 7.5 gotchas doc as a predicted future risk (`voiceNotes?`) — the fake API stub's `attachInspectionVoiceNote` handler needed to echo back the full current `voice_notes[]`, not an empty list, via a new `_voiceNoteFrom(request)` helper. 3 new tests in `inspection_detail_screen_test.dart`: an honest empty state when no voice notes exist yet; a synced voice note renders its upload-progress count and formatted duration; a locally-queued voice note renders its upload-state badge and duration, counted as pending. A real bug these tests caught before merge (see the Phase 7.6 architecture section): `InspectionMediaSection`'s photo/video gallery read the shared `MediaQueue` stream without filtering out `kind == 'audio'`, so a queued voice recording rendered as a broken tile in the wrong grid — fixed by filtering both sections to their own kind. New `apps/mobile/lib/media/voice_recording_screen.dart` (record/stop/preview/re-record/save, a pulsing level meter, mic-permission-denied graceful state, all behind an injectable `recorderBuilder` seam mirroring `MediaCaptureScreen`'s) and `apps/mobile/lib/media/inspection_voice_notes_section.dart` (list with playback/duration/checklist-link/remove, unifying synced and queued items behind one `_VoiceItem` shape mirroring `_GalleryItem`). `ApiContract` gained `attachInspectionVoiceNote`/`updateInspectionVoiceNote`/`detachInspectionVoiceNote` as real interface methods (7.1's established pattern), requiring all 13 hand-rolled `FakeApi`/`FakeSyncApi`/`FakeQrApi`/`_IdentityApi` test doubles to gain stubs. Admin: ESLint reported no warnings/errors; the full `vitest run` reported 202 passed, 6 credential-only skips (up from 200); `next build` compiled all 29 routes cleanly. New read-only **Voice notes** section in the existing `InspectionDetailPage`, below Media, listing each voice note's native `<audio controls>` element (a signed URL), formatted duration, and its linked checklist item's label. 2 new tests in `inspection-detail-page.test.tsx`: an honest empty state with zero voice notes; a voice note renders its `<audio>` element with the correct `src`/`controls`, its formatted duration, and its linked checklist item's label. Real-creds proof against `thinking-case-469504-c0` (`apps/api/scripts/verify_voice_note_roundtrip.py`, mirroring `verify_annotation_roundtrip.py`'s precedent): created a real probe inspection, uploaded a real (probe) audio blob directly to Storage under the `voice/` path and attached it, confirmed it landed in `voice_notes[]`, fetched its real signed URL over HTTP and confirmed the response bytes matched exactly what was uploaded (genuine playback proof, not just a structural check), re-fetched the inspection and confirmed the round-trip, linked it to a checklist item, detached it, and cleaned up both the Storage blob and the probe inspection — all against the live project, `PASS` end to end. The mobile-side recording feel (microphone gesture, level-meter responsiveness, actual audio playback quality) is covered by the widget tests above for wiring correctness only; verifying it feels good on a real device requires physical hardware, documented as a manual follow-up in the guide below rather than faked here. GitHub Actions run 31082166944 passed Admin (1m42s), Mobile (2m58s), Contracts (2m19s), and API (43s) — one rerun needed after an unrelated, pre-existing flaky ambiguous-text assertion in `dashboard-page.test.tsx` (ISO to `main`, untouched by this phase; see `fev-admin-vitest-full-suite-flake` for the same class of local full-suite flakiness). PR: https://github.com/umerali06/EnergyVerse/pull/34. Phase 7.6 is now **Done**; the rest of Phase 7 (readings, signature, AR, AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-08-06 |
| 7.7 — manual status readings (resolves the deferred §9 manual-status log) | Backend: Ruff and strict mypy (91 source files) passed; `poetry run pytest` reported 317 passed, 3 credential-only skips, 0 failures — 14 new tests in `test_inspections.py`: readings persist with server-stamped `recorded_at`/`recorded_by` (client-sent values for both are ignored); `condition` is required (missing it 422s at the request-validation layer); a fresh readings save fully replaces the previous one rather than merging; readings are locked once the inspection is completed (same `inspection_locked` 409 as any other field); readings survive an unrelated `PATCH` (e.g. `notes`) byte-for-byte; all 5 condition→asset-status mappings verified parametrized (`Excellent`/`Good`→`Healthy`, `Fair`/`Poor`→`Warning`, `Critical`→`Critical`); completing an inspection with no readings at all leaves the asset's `current_status` untouched; a `PATCH` setting readings on a still-`draft`/`in_progress` inspection never rolls up the asset (only `complete_inspection` does); the rollup writes a distinct `asset.status_rolled_up` audit entry (`from`/`to`/`inspection_id` metadata); and a full cross-check that completing a Critical-condition inspection moves the real 4.4 dashboard `assets-summary` Critical/Healthy counts by exactly 1 each. New `Readings` entity (`Inspection.readings: Readings | None`, replacing the 7.1-era untyped `dict` placeholder, with a `field_validator` normalizing the legacy `{}` to `None` on load) and `ReadingsResponse`/`ReadingsInput` wire models. No new routes — `readings` rides the existing `PATCH /inspections/{id}` alongside `checklist_responses` (D-059), stamped server-side in `update_inspection` the same way `answered_at`/`answered_by` already are. New `AssetRepository.roll_up_status_from_inspection` (mirrors `backfill_qr_code`'s narrow-update-plus-explicit-audit shape) is called from `complete_inspection` only, after the lifecycle transition succeeds, via a new `READINGS_CONDITION_TO_ASSET_STATUS` mapping. `test_openapi.py`'s golden operation set is unchanged (no new operations, just two new/changed models). Contracts regenerated for `ReadingsInput`/`ReadingsResponse` and the updated `InspectionDetail`/`UpdateInspectionRequest`, confirmed drift-clean. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 221 tests passed (up from 214). New `readings (Phase 7.7)` group in `local_inspections_repository_test.dart` (4 tests): `updateInspection(readings:)` writes the local blob and coalesces into the existing `update` outbox row rather than a dedicated mutation type; an unrelated field edit resends the previously-saved readings unchanged (the outbox payload's `readings` field, not just the local cache, is asserted); a fresh readings save fully replaces the previous one; readings recorded offline survive a real app restart (on-disk Drift file across two `AppDatabase` instances, mirroring 7.2/7.4/7.5's own restart-persistence tests). New `readings sync (Phase 7.7)` group in `sync_engine_test.dart` (2 tests) proving the same fake-echo-full-state trap flagged in the Phase 7.5/7.6 gotchas (a fake `updateInspection` stub that echoes empty readings would wipe a not-yet-synced value) also applies here, via a new `_readingsResponseFrom(request)` helper. New `readings (Phase 7.7)` widget test in `inspection_detail_screen_test.dart` plus an extended assertion block on the existing "completed inspection" test: unit labels (`Temperature (°C)`/`Pressure (bar)`/`Noise level (dB)`) are visible, nothing autosaves until `condition` is chosen, and a later field builds on the already-saved condition; the completed-inspection read-only render shows the condition prominently via a `StatusPill`, every populated value with its unit, and no editable inputs. New `apps/mobile/lib/inspections/inspection_readings_section.dart` (condition select with a health-colored `StatusPill`, unit-labeled numeric fields with numeric keypads, a leak yes/no button pair mirroring the checklist's boolean Pass/Fail idiom, operational-status/priority selects, comments/recommendations text areas) replaces the 7.3 "Readings — Coming soon" row; autosaves through a new optional `readings:` parameter on the existing `LocalInspectionsRepository.updateInspection` (no new outbox mutation type); a new nullable `readings` JSON-blob column on `LocalInspections` (schema v5→v6). One pre-existing widget test needed a fix caused by this phase's own new form fields, not a regression: `scrollUntilVisible`'s default `find.byType(Scrollable)` became ambiguous once the readings section's several `AppTextField`s each contributed their own internal scrollable, so the "answering the required item enables Complete" test now passes an explicit `scrollable:` finder scoped to the page's own outer `ListView`. Admin: ESLint reported no warnings/errors; `next build` compiled all 29 routes cleanly (bundle budget unchanged); `vitest run` reported this file's 16 tests passing (3 new: an honest empty state with no readings; a fully populated read-only render — condition via `StatusPill`, every value with its unit, priority badge, leak badge, comments, recommendations; priority/leak badges correctly omitted when unset) — a pre-existing, unrelated full-suite flake in `company-settings-page.test.tsx` reproduced and cleared on an immediate retry with no code change (see `fev-admin-vitest-full-suite-flake`), same class of local-machine flakiness as 7.6's. New read-only **Readings** section in the existing `InspectionDetailPage`, below Voice notes, reusing the inspections list's own `StatusPill`/tone-mapping convention for the condition. Real-creds proof against `thinking-case-469504-c0` (`apps/api/scripts/verify_readings_rollup.py`, mirroring `verify_voice_note_roundtrip.py`'s precedent): captured the seeded Feed Pump asset's real starting `current_status` and the real dashboard Critical-Assets count, completed a real probe inspection with `condition=Critical` and confirmed both the asset's `current_status` became `Critical` and the live dashboard count increased by exactly 1, then completed a second probe inspection with the condition that maps back onto the asset's original status and confirmed both were fully restored — all against the live project, `PASS` end to end; both probe inspections cleaned up. The mobile-side form's on-device feel (numeric-keypad ergonomics, dropdown usability) is covered by the widget test above for wiring correctness only; verifying it feels good on a real device requires physical hardware, documented as a manual follow-up in the guide below rather than faked here. GitHub Actions run 31103974055 passed API (46s), Admin (1m48s), Contracts (2m29s), and Mobile (3m13s) — one rerun needed after a ruff line-length (E501) fix in the new dashboard-KPI cross-check test (two lines over 100 chars, missed by the earlier targeted per-file ruff runs). PR: https://github.com/umerali06/EnergyVerse/pull/35. Phase 7.7 is now **Done**, resolving the §9 manual-status-log deferral first noted in Phase 4.1; the rest of Phase 7 (signature, AR, AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-08-06 |
| 7.8 — digital signature (inspector sign-off) | Backend: Ruff and strict mypy (96 source files) passed; `poetry run pytest` reported 325 passed, 3 credential-only skips, 0 failures (328 collected total) — 8 new tests in `test_inspections.py`: signature strokes/role persist on a successful complete; `strokes` is required (missing it 422s); `expected_revision` is required; a stale `expected_revision` is rejected with 409 `revision_conflict` (not `checklist_incomplete`, proving the revision check runs first); a client-supplied `signer_uid`/`signer_name`/`signer_role` in the request body is silently ignored — the persisted signature always shows the real authenticated identity; the signature is bound to the inspection's own post-completion revision; completing with no body at all 422s (no way to complete unsigned); and a completed inspection's signature survives a fresh `GET`. New `Signature`/`SignatureStroke` entities (`Inspection.signature: Signature | None`, replacing the 7.1-era untyped `dict` placeholder) and `SignatureResponse`/`SignatureStrokeResponse`/`SignaturePointResponse`/`CompleteInspectionRequest`/`SignatureStrokeInput`/`SignaturePointInput` wire models. `POST /inspections/{id}/complete` now requires a body; `InspectionService` gained a `UserRepository` dependency purely for the signer's `display_name` lookup (identity itself comes from `current_user.uid`/`role_key`, never the request). `test_openapi.py`'s golden operation set is unchanged (no new operations, one route gained a request body, six new models). Contracts regenerated for the new models and the updated `InspectionDetail`/`complete_inspection` operation, confirmed drift-clean — a real bug was found and fixed mid-phase: the first implementation used a raw `list[list[AnnotationPoint]]` for strokes, which threw `StateError: No builder factory for BuiltList<SignaturePointInput>` at Dart runtime the moment a mobile test actually serialized it (the pinned generator emits a builder factory for the outer nested-list level but not the inner one); switching to a named `SignatureStroke{points: [...]}` wrapper (one level of nesting, matching `Annotation.points`'s already-proven shape) fixed it and both generated clients were regenerated clean. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 224 tests passed (up from 221) — 2 new tests in `local_inspections_repository_test.dart` (`completeInspection` writes the drawn strokes to a new local-only `pendingSignatureStrokes` column and the correct `expected_revision`-carrying outbox payload; the drawn strokes and `completed` status survive being re-read from a fresh `AppDatabase` instance, i.e. an app restart) plus a new `markConflict`-reverts-a-stale-complete's-optimistic-status test; 1 new widget test in `inspection_detail_screen_test.dart` (the signature pad's Clear button resets the drawing and re-disables Sign & Complete; dismissing the sheet without signing leaves the inspection genuinely untouched) plus extended assertions on the two existing checklist/completion tests (drawing a stroke via a simulated drag, confirming Sign & Complete enables/disables correctly, and confirming the completed row's `signature`/`pendingSignatureStrokes` columns) and on the "completed inspection renders read-only" test (the SIGNATURE section shows signer name/role and a read-only strokes preview, with no drawable canvas). New `SignaturePad`/`SignaturePadController`/`SignaturePreview` (`apps/mobile/lib/inspections/signature_pad.dart`) — a `CustomPaint` freehand canvas normalized like the 7.5 annotation canvas, no tool palette; `LocalInspectionsRepository.completeInspection` now requires a `strokes` parameter; `LocalInspections` gained `signature`/`pendingSignatureStrokes` columns (schema v6→v7). Admin: ESLint reported no warnings/errors; `vitest run` on this file reported 19 tests passing (3 new: an honest empty state when unsigned; a fully populated render — signer name/role, signed-at timestamp, "Signed" + "Valid at revision N" badges, and an SVG stroke preview; a "Superseded (signed revision X, now at Y)" indicator once the live revision moves past the signed one) — the full `pnpm test` suite (214 tests) reproduced one unrelated, pre-existing full-suite-only timeout flake in `checklist-template-form-page.test.tsx` that passed cleanly on an immediate isolated re-run with no code change (same class of local-machine flakiness as 7.6/7.7's own documented flakes, a different file this time). New read-only **Signature** section in the existing `InspectionDetailPage` (new `SignaturePreview` SVG component, mirroring `AnnotationOverlay`'s normalized-`viewBox` convention) below Readings. Real-creds proof against `thinking-case-469504-c0` (`apps/api/scripts/verify_signature_completion.py`): completed a real probe inspection with a real signature and confirmed the persisted signer name/role exactly matched the real seeded `field_inspector` account's `display_name` (server-derived, ignoring a deliberately spoofed `signer_uid`/`signer_name` sent in the same request) and that `signature.inspection_revision` equalled the completed inspection's own revision; then reproduced the offline pre-completion race on a second probe inspection (edited it to bump its revision, then attempted to complete-with-signature against the now-stale revision) and confirmed the server rejected it with 409 `revision_conflict`, and that re-signing against the current revision then succeeded — all `PASS` end to end against the live project; both probe inspections cleaned up. GitHub Actions run TBD — filled in once CI is green. PR: TBD. Phase 7.8 is now **Done**; the rest of Phase 7 (AR, AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-08-06 |
| 7.9 — AR/manual dimension measurement | Session opened on an already-uncommitted Step-1 `ar_flutter_plugin_2` spike (D-062) gated behind physical-device validation before the full feature could be built; the product owner was asked directly and chose to waive that gate (D-063) rather than write a device test guide or drop AR for manual-only, so this phase built the full feature (data model, offline sync, manual fallback, screenshot evidence, admin review) directly. Backend: Ruff and strict mypy (87 source files) passed; `poetry run pytest` reported 338 passed, 3 credential-only skips, 0 failures — 12 new tests in `test_inspections.py`: a manual measurement persists with no screenshot/points and never bumps revision; an AR measurement persists with its screenshot reference; an AR measurement without points still succeeds (points are optional overlay markers, not a hard requirement — D-064); an AR measurement missing its screenshot 422s (`ar_measurement_missing_screenshot`); a measurement referencing an unknown `media_local_id` 404s (`media_not_found`); idempotent-by-id replay is a no-op; a conflicting replay under the same id 409s (`ar_measurement_conflict`); cross-tenant access 404s; `update` edits `label`/`note`/`checklist_item_id` only, preserving `distance_meters`; `update` is a no-op on a missing id; `delete` removes it and is idempotent on replay; and a measurement survives an unrelated checklist-autosave `PATCH` byte-for-byte. New `ArMeasurement` entity (`Inspection.ar_measurements: list[ArMeasurement]`, replacing the 7.1-era untyped `list[dict]` placeholder) and `ArMeasurementResponse`/`CreateArMeasurementRequest`/`UpdateArMeasurementRequest` wire models. Three new routes mirror 7.5's annotation mutation protocol exactly (idempotent by client-generated id, no `expected_revision`); `InspectionRepository.append_ar_measurement`/`update_ar_measurement`/`remove_ar_measurement` are a field-for-field copy of the annotation repository methods. `test_openapi.py`'s golden operation set updated for the three new operation IDs. Contracts regenerated for the new models and the updated `InspectionDetail`, confirmed drift-clean. A genuine mid-phase design correction, not a late-discovered bug: `method="ar"` was initially specced to require exactly two tapped points alongside the screenshot; relaxed to screenshot-only once it became clear `ar_flutter_plugin_2`'s hit-test callback carries no 2D screen-tap coordinate to populate them honestly from. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 236 tests passed (up from 224) — 7 new tests in `local_inspections_repository_test.dart` (manual measurement writes optimistically and queues `create_measurement`; an AR measurement carries its screenshot reference and points; `update` edits label/note preserving distance; `update` is a no-op on a missing id; `delete` removes it and queues `delete_measurement`; a measurement recorded offline survives a real app restart) and 6 new tests in `sync_engine_test.dart` (`create`/`update`/`delete`-measurement each dispatch through the record outbox and clear it on success, with `update`/`delete` asserting the exact `measurement_id` + request wrapper payload) plus 3 new widget tests in `inspection_detail_screen_test.dart` (an honest empty state with no measurements; a rendered measurement showing its formatted distance/label/note; a completed inspection hides the Add-measurement action) — the new tests needed the same `scrollUntilVisible`-ambiguity fix documented in the 7.7 gotchas (an explicit `scrollable:` finder scoped to the page's own outer `ListView`), since MEASUREMENTS sits below the 7.7 readings section's several `AppTextField`s. New `ArMeasurementScreen`/`ManualMeasurementScreen` (`apps/mobile/lib/ar/`) replace the Step-1 spike screen (deleted); `ArMeasurementScreen` captures a screenshot via `ARSessionManager.snapshot()` (extracting bytes from its `MemoryImage` return) and reuses the unmodified 7.4 `LocalMediaRepository.enqueueCapture`; "Enter manually instead" stays reachable from every state, not gated behind an error. New `InspectionMeasurementsSection` wires both screens' results into `LocalInspectionsRepository.createMeasurement`. Three new `OutboxMutationType` values (`create_measurement`/`update_measurement`/`delete_measurement`) and a new `LocalInspections.arMeasurements` column (schema v7→v8). Admin: ESLint and `next lint` reported no warnings/errors; `vitest run` on this file reported 22 tests passing (4 new: an honest empty state with no measurements; a manual measurement rendering its method badge/formatted distance/label/note; an AR measurement under 1 meter formatting in centimeters) — the full `pnpm test` suite (211 passed, 6 credential-only skips) reproduced the pre-existing, unrelated full-suite-only teardown flake in `company-settings-page.test.tsx` already documented in `fev-admin-vitest-full-suite-flake` (a toast `setTimeout` firing after test-environment teardown), same class of local-machine flakiness as every prior phase's own documented flake, cleared on isolated re-run with no code change. New read-only **Measurements** section in the existing `InspectionDetailPage`, below Readings, with a `formatMeasurementDistance` helper (centimeters under 1m, meters otherwise) mirrored field-for-field on mobile. Real-creds proof against `thinking-case-469504-c0` (`apps/api/scripts/verify_ar_measurement_roundtrip.py`): created a real manual measurement and a real AR measurement referencing a real uploaded screenshot blob, confirmed both re-fetch correctly from Firestore (sync round-trip), edited the manual measurement's note while confirming its distance stayed untouched, deleted the AR measurement while confirming the manual one survived — `PASS` end to end against the live project; probe inspection and blob cleaned up. Residual, explicitly accepted risk (D-063): `ar_flutter_plugin_2`'s plane detection/measurement accuracy on real ARCore/ARKit hardware remains unverified — confined to the AR capture screen only, per D-062's own escape hatch if it proves inadequate. GitHub Actions run 31363748651 passed API (49s), Admin (1m52s), Contracts (2m16s), and Mobile (3m0s). PR: https://github.com/umerali06/EnergyVerse/pull/37. Phase 7.9 is now **Done**; the rest of Phase 7 (AI analysis, admin review) remains to be scoped phase-by-phase. | Passed (CI-safe + real-creds) | 2026-08-10 |
| 7.10 — AI photo analysis | Continued in the same session once the product owner confirmed scope directly (build 7.10 now; Claude vision API only, no separate CV model; on-demand "Analyze" action, never automatic on upload). Backend: Ruff and strict mypy (91 source files) passed; `poetry run pytest` reported 347 passed, 3 credential-only skips, 0 failures — 12 new tests in `test_inspections.py` against a `FakeAiClient` (see `tests/fakes/ai.py`, no real Anthropic key needed): a manual-equivalent analysis run persists its summary/recommendations/risk-level and never bumps revision; a run with findings creates matching `Annotation(source="ai", confidence, color=#7C3AED)` records plus one `AiAnalysis` record referencing their ids; a run with zero findings still persists a real `AiAnalysis` with an empty `annotation_ids` (this is what surfaced the `ArrayUnion`-rejects-empty-list bug, fixed in `append_ai_analysis`); analyzing an unknown media id 404s (`media_not_found`); analyzing a video 422s (`ai_analysis_unsupported_media_kind` -- video is out of scope this phase, no frame-extraction pipeline exists); an upstream `AiClientError` from the vision client 502s (`ai_analysis_failed`) and leaves no partial annotations/analysis behind; cross-tenant access 404s; `review` stamps `reviewed`/`reviewed_by`/`reviewed_at` server-side and is idempotent-on-missing; and an analysis survives an unrelated checklist-autosave `PATCH` byte-for-byte. New `AiAnalysis` entity (`Inspection.ai_analysis: list[AiAnalysis]`, replacing the 7.1-era untyped `dict | None` placeholder) and `AiAnalysisResponse` wire model. First third-party HTTP/SDK integration in this backend: new `app/ai/vision_client.py` (`ClaudeVisionClient`, `anthropic` SDK, forced tool-use via a `report_photo_analysis` tool schema for reliable structured JSON) behind a `VisionAnalysisClient` protocol `InspectionService` actually depends on. New `Settings.anthropic_api_key`/`ai_vision_model` (default `claude-sonnet-5`); new `InspectionMediaStorage.download_bytes()` (the one place this class reads media bytes back into the API process, breaking its own long-standing "bytes never pass through this backend" precedent on purpose). Two new routes (`analyze`, `review`) match neither the create/update/delete nor attach/detach shape used elsewhere, since "analyze" is a computation trigger, not a CRUD create. `test_openapi.py`'s golden operation set updated for the two new operation IDs. Contracts regenerated for `AiAnalysisResponse`/`CreateArMeasurementRequest`-sibling models and the updated `InspectionDetail`, confirmed drift-clean. Mobile: `flutter analyze` reported no issues; `flutter test` reported all 246 tests passed (up from 236) -- 3 new tests in `local_inspections_repository_test.dart` (`analyzeMedia` calls the API directly without touching the outbox and upserts the result; `analyzeMedia` propagates an API error and leaves the local cache untouched; `reviewAiAnalysis` calls the API directly and upserts the reviewed result) and 4 new widget tests in `inspection_detail_screen_test.dart` for the new `InspectionAiAnalysisSection` (hidden when nothing has been analyzed; a rendered run showing its summary/risk-level/recommendations and a Mark-reviewed action; the action hidden once already reviewed). New `AnnotationCanvasScreen` app-bar "Analyze with AI" action (only offered once a photo has synced, since the route needs a real server `media_id`) and new `InspectionAiAnalysisSection`; both `LocalInspectionsRepository.analyzeMedia`/`.reviewAiAnalysis` are direct, immediate, online-only calls -- the first mutations in this repository that deliberately bypass the offline outbox, since there's no honest optimistic value to echo before a live AI call completes. New `LocalInspections.aiAnalysis` column -- this migration also retroactively fixed a real gap from Phase 7.9 found while adding it: `arMeasurements` had been added without ever bumping `schemaVersion` or its own `onUpgrade` branch, so an already-installed app would never have received that column; both are now correctly migrated (v7→v9). Admin: ESLint and `next lint` reported no warnings/errors; `next build` compiled all 29 routes cleanly with the bundle budget unchanged (342.8 KB); `vitest run` on this file reported 25 tests passing (4 new: an honest empty state with nothing analyzed; a rendered run with its risk-level badge and needs-review status; the reviewed state hiding "Needs review") -- the full `pnpm test` suite (214 passed, 6 credential-only skips) ran clean this time with no recurrence of the documented `company-settings-page.test.tsx` full-suite flake. New read-only **AI analysis** section in the existing `InspectionDetailPage`, below Media -- no capture/trigger action on admin, matching every other Phase-7 sub-resource's admin-is-read-only-reviewer precedent. The real Claude API call against the live Anthropic API is **unverified this phase** -- no `ANTHROPIC_API_KEY` was available in-session; the product owner chose to proceed without one rather than block on adding it, so a real-creds proof (mirroring `verify_ar_measurement_roundtrip.py`) is an explicit open follow-up once a key is configured. Every other behavior (routing, validation, persistence, error translation, offline-outbox exclusion) is fully covered against a `FakeAiClient`/fake API layer. GitHub Actions run 31405158916 passed API (48s), Admin (1m53s), Contracts (2m20s), and Mobile (3m8s). PR: https://github.com/umerali06/EnergyVerse/pull/38. Phase 7.10 is now **Done**, and with it Phase 7 (data model, offline sync, capture, checklist, readings, signature, AR, AI analysis, admin review) is now **COMPLETE** per its own scope description in `PHASE_TRACKER.md`. | Passed (CI-safe; real Claude call deferred) | 2026-08-10 |
| 8.1 — work order data model and backend CRUD/lifecycle | With Phase 7 complete and Phases 5/6/8+ still "To be defined," the product owner was asked directly rather than have scope invented: first to pick from the original spec's remaining sections, then specifically "Work Orders" (§12) over Permit-to-Work/Notifications; also chose to merge the then-open PR #38 and branch `phase/8.1-work-order-model` from updated `main` before starting, and to add `work_orders.close` as a dedicated permission so a technician cannot self-close their own work order. Backend: Ruff and strict mypy (104 source files) passed; `poetry run pytest` reported 376 passed, 3 skipped, 0 failures (one unrelated live-Firebase-auth network test hit a transient `RemoteDisconnected` on the full-suite run and passed cleanly on an isolated retry -- not a regression, nothing in this phase touches auth) -- 30 new tests in `test_work_orders.py`: tenant isolation on list/get, a table-driven RBAC sweep across all 7 system roles for both read and write routes plus a dedicated sweep proving `close` is gated by `work_orders.close` specifically (not `work_orders.write` -- `maintenance_technician` 403s, `operations_manager` succeeds), create (unknown-asset 404, duplicate-id 409, correct defaults), list filters (asset_id/facility_id/status/technician_id) and cursor pagination across 5 seeded work orders, assign (from `open`, reassign from `assigned`, invalid transition from `in_progress`, stale-revision 409), accept (wrong-technician 403 `not_assigned_technician`, invalid-transition 403, success moves to `in_progress`), submit-for-review (wrong-technician 403, invalid-transition 409, stale-revision 409, success moves to `pending_review`), close (technician 403, supervisor success, invalid-transition 409), cancel (from `open` and `pending_review`, invalid-transition once `closed`), and soft delete. Four pre-existing golden tests were updated for the new permission catalog entry (23→24 keys), the new `operations_manager`/`company_admin`/`super_admin` expected permission sets, the new `work_orders` seed count (5) and `client.counts()` key, and the 9 new operation IDs in `test_openapi.py`'s stable list; `test_seed_and_rbac.py`'s tenant-isolation test also gained a `work_orders` assertion mirroring every other collection type already covered there. New `WorkOrder`/`WorkOrderCreate` entities, `WorkOrderRepository` (lifecycle mirrors `InspectionRepository.apply_lifecycle`'s `RevisionConflictError`/`InvalidTransitionError` shape exactly), `WorkOrderService` (with the `accept`/`submit-for-review` self-only 403 check independent of the route-level permission split), and a 9-route router. `scripts/seed.py` gained `MAINTENANCE_TECHNICIAN_UID`, a `DemoWorkOrderSeed` dataclass, 5 fixtures spanning `open`/`assigned`/`in_progress`/`pending_review`/`closed`, and `_ensure_work_order()` (a structural mirror of `_ensure_inspection()`, seeded sequentially for the same same-document-race reason). OpenAPI export and the pinned TypeScript Fetch/Dart Dio clients were regenerated for `WorkOrderListItem`/`WorkOrderDetail`/`CreateWorkOrderRequest`/`AssignWorkOrderRequest`/`SubmitWorkOrderForReviewRequest`/`WorkOrderDeleted` and the 9 new `WorkOrdersApi` operations, confirmed drift-clean with `git status` showing no collateral changes outside `packages/contracts`. No UI was built this phase (backend-only, mirroring Phase 4.1's precedent of shipping the data model/CRUD a full sub-phase ahead of any capture UI), so there is no browser/screenshot evidence to capture; mobile/admin work-order screens are deferred to future sub-phases. Real-creds proof (`verify_work_order_roundtrip.py`) against the live Firebase project `thinking-case-469504-c0`: created a real work order, confirmed a supervisor's accept attempt is rejected with `not_assigned_technician`, walked assign→accept→submit-for-review→close end to end with the real assigned technician, exercised an independent create→cancel path, and cleaned up both probe documents. `reconcile_roles.py --company-id cmp_feee017b83914cdd8323745e6359cc32` was run against the one real non-demo tenant, backfilling `work_orders.close` onto its `operations_manager` role (verified via the script's own printed role-id output), per the same precedent as every prior permission addition (D-029/D-036/etc.). GitHub Actions run 31460580286 passed API (51s), Admin (1m46s), Contracts (2m39s), and Mobile (3m20s). Draft PR: https://github.com/umerali06/EnergyVerse/pull/39. Phase 8 is now **In progress**; mobile/admin work-order UI (8.2+) remains to be scoped with the product owner. | Passed (CI-safe; backend-only, no UI this phase) | 2026-08-11 |
| 8.2 -- work order mobile + admin UI | Product owner confirmed scope directly (mobile + admin together, mirroring every Phase 7 sub-phase's mobile-capture-plus-admin-review bundling; offline-first for the technician's accept/submit-for-review actions, matching inspections' D-046/D-047 precedent over a Phase 7.10-style online-only call). No backend/contract changes -- Phase 8.1 already shipped the complete API surface and generated clients. Mobile: new `LocalWorkOrders`/`WorkOrderOutbox` Drift tables (schema v9->v10) and a separate `WorkOrderSyncEngine` (D-067) rather than extending inspections' engine -- only `accept`/`submit_for_review` ride the offline outbox, since assign/close/cancel/create/delete are always-online supervisor actions from admin. Two real correctness gaps were found and fixed while writing the sync-engine tests, not late-discovered: `accept`'s already-applied check needed to compare the fresh server `technicianId` against this device's own still-unmutated local row (not just a status check) to correctly distinguish "my own accept already landed" from "someone else was reassigned and accepted while I was offline"; and `markConflict` was overwriting the technician's own submitted `completionNotes`/`laborHours`/`materialsUsed` from the server snapshot, which would have destroyed exactly the content `resolveConflict(keepLocal: true)` needs to re-submit -- fixed to only sync status-machinery fields, mirroring inspections' own `markConflict` posture. `flutter test` reported all 257 tests passed -- 15 new: 8 in `work_order_sync_engine_test.dart` (accept/submit dispatch, both already-applied paths, both genuine-conflict paths, and both `resolveConflict` directions), 4 in `work_orders_screen_test.dart` (loading/empty/403, and the "assigned to me" toggle re-fetching without a technician filter), and 3 in `work_order_detail_screen_test.dart` (Accept task visibility, accepting flips optimistically to in_progress, submitting for review flips optimistically to pending_review); `flutter analyze` clean (one pre-existing-pattern cosmetic warning on a private `_InfoRow` widget, matching `inspection_detail_screen.dart`'s own identical convention). New `WorkOrdersScreen` (defaults to "assigned to me", status filter, pending-outbox indicator) and `WorkOrderDetailScreen` (Accept task/Submit for review offered only to the actually-assigned technician, two-button conflict sheet mirroring inspections'); the mobile bottom-nav "Work" destination's `comingSoon` placeholder is replaced by the real screen. One pre-existing `app_shell_test.dart` case ("tapping a bottom destination routes to its Coming soon page") needed retargeting a second time -- Phase 4.2 already retargeted it once from `/assets` to `/work-orders`; now that Work Orders is also real, it asserts the real screen renders and the bottom-nav tab activates instead, since no primary destination is left `comingSoon` at all. Admin: new `work-orders-page.tsx` (list, filters, create modal) and `work-order-detail-page.tsx` (assign modal, completion-details review section, close/cancel actions) mirroring the Assets/Users page and modal conventions; Close is gated by `work_orders.close` specifically, distinct from Assign/Cancel's `work_orders.write` gate -- the UI mirror of D-066's route-level permission split; admin never calls accept/submit-for-review at all (technician-only, self-only, enforced server-side). `pnpm vitest run src/work-orders` reported 22 new tests passed (loading/empty/error/403, filter re-fetch, priority client-side filtering, create/assign flows, cursor pagination); `next lint` clean; `next build` compiled all 31 routes including the new `/work-orders` and `/work-orders/[id]`, bundle budget unchanged (342.8 KB); the full `pnpm test` suite reported 236 passed, 6 credential-only skips, no recurrence of the documented `company-settings-page.test.tsx` full-suite flake this run. A real, contained incident occurred mid-phase: the background agent building the admin half ran `git stash` (with untracked files included) against the same shared working tree to get a clean tsc baseline, momentarily reverting every uncommitted file on disk including this session's own in-progress mobile work; no work was actually lost (the stash captured everything and was recovered via `git stash pop`), but it is recorded in DECISIONS.md as a standing lesson against running a concurrent agent with git/shell access on an uncommitted shared tree without committing first or isolating it in its own worktree. GitHub Actions run 31500005717 passed API (47s), Admin (1m35s), Contracts (2m36s), and Mobile (2m50s). Draft PR: https://github.com/umerali06/EnergyVerse/pull/40. Phase 8 (data model, mobile technician flow, admin supervisor flow) is now **COMPLETE** per its own scope description in `PHASE_TRACKER.md`. | Passed (CI-safe) | 2026-08-11 |
| 5.1 -- safety-report data model and backend lifecycle | Added the strict tenant-scoped incident entity, seven lifecycle endpoints, D-068 RBAC split, revision conflicts, filtering/pagination, soft deletion, and before/after audits. Focused safety/RBAC/OpenAPI verification: 11 passed. Full API regression: 382 passed, 3 credential-only skips. Ruff clean; strict mypy clean across 97 source files. Pinned TypeScript Fetch and Dart Dio clients regenerated successfully with `SafetyReportsApi` plus seven typed models. Dart analyzer reported only the generator's three pre-existing warnings, with no safety-report errors. | Passed (local, CI-safe) | 2026-08-16 |
| 5.2 -- safety evidence and corrective actions | Focused safety/RBAC/OpenAPI verification: 14 passed. Full API regression: 385 passed, 3 credential-only skips. Ruff and strict mypy clean. Tests cover private signed evidence upload/delete, reporter/manager ownership, MIME/path behavior, corrective-action self-service, cross-role denial, mandatory completion/cancellation notes, and the no-unfinished-actions resolution gate. OpenAPI plus TypeScript Fetch/Dart Dio clients regenerated successfully for five new operations and expanded typed models. | Passed (local, CI-safe) | 2026-08-16 |
| 5.3a -- admin HSE safety interface | Focused safety UI: 2 passed (real list/detail rendering and controlled HSE transition). Full admin regression: 238 passed, 6 credential-only skips across 24 passing test files. ESLint clean. Next.js production build compiled all 29 routes; `/safety` is a real 3.94 kB route and the shared first-load bundle remains 102 kB. Tests/build use the typed client seam; no mock data exists in production code. | Passed (local, CI-safe) | 2026-08-16 |
| 5.3b.1 -- mobile safety offline foundation | Drift schema advanced v10→v11 with isolated safety cache/outbox tables and generated database bindings. Focused repository tests: 2 passed, covering atomic offline report+mutation persistence with client UUID/trimmed operator input/wire values/GPS and retry-backoff scheduling with explicit manual bypass. Focused Flutter analysis of API, database, app lifecycle, safety repository/engine, and tests reported no issues. Production code contains no fixtures or mock data; the unavailable API adapter exists only as a compatibility seam for legacy test doubles and always fails closed as a network error. | Passed (local, CI-safe) | 2026-08-16 |
| 5.3b.2 -- mobile safety list/detail/report screens | Focused mobile safety widget tests: 2 passed sequentially against a real in-memory Drift cache, covering cached directory rendering, severity/local-sync indicators, `safety.write` action visibility, and offline detail rendering of description/GPS/revision. The earlier apparent runner hang was diagnosed precisely: direct Drift setup needed `tester.runAsync`, the design theme provider was required by shared skeleton primitives, labels use uppercase by design, and `SafetySyncEngine` had to be disposed before Flutter's pending-timer invariant. Repository tests remained 2/2 passing. Focused Flutter analysis of safety screens/controllers/routes/tests reported no issues. | Passed (local, CI-safe) | 2026-08-16 |
| 5.3b.3 evidence foundation (intermediate) | Drift schema advanced v11→v12 with isolated `SafetyEvidenceQueue` and corrective-action target IDs. Focused evidence repository tests: 2 passed, proving a real temporary file's existence/MIME/actual byte length is validated before durable enqueue and that queued video remains ineligible until its offline parent report is synced. Focused Flutter analysis of API/database/all safety sources and the new test reported no issues. This is an intermediate gate only; 5.3b.3 remains In progress pending lifecycle integration, capture/action UI, replay tests, and conflict UX. | Passed (intermediate, local) | 2026-08-16 |
| 5.3b.3 -- mobile safety evidence, assigned actions, and replay | Final focused safety coverage totals 9 tests: 3 report/action repository tests, 2 evidence validation/parent-order tests, 2 list/detail widget tests (including assignee-only Start action), and 2 direct worker tests proving successful multipart queue removal and targeted corrective-action dispatch with authoritative server replacement. The complete mobile regression suite passed all 266 tests sequentially. Full `flutter analyze` reported no issues; `git diff --check` passed. Production capture supports camera/gallery photo/video, persists picker output into app documents before enqueue, rejects unsupported formats rather than relabeling them, enforces backend-aligned byte limits, cleans copied files on persistence failure/success/discard, and wires both workers through app resume/disposal and authenticated-user cache wiping. | Passed (full mobile regression, local) | 2026-08-19 |
| 5.4 -- safety dashboard integration (Phase 5 complete) | Backend: focused safety/OpenAPI verification passed; Ruff and strict mypy clean across 98 source files; full regression 389 passed/3 credential-only skips. Tests prove real category/total aggregation, all-role `safety.read` gating, tenant isolation, and soft-delete exclusion. Pinned OpenAPI/TypeScript/Dart clients regenerated. Admin: 3 focused safety-widget tests and 16 dashboard tests passed; ESLint clean; production build compiled all 29 routes. Full Vitest executed all tests (241 passed/6 credential-only skips) then reproduced the documented unrelated ToastProvider teardown-timer error; the originating company-settings file passed 8/8 in isolation. Mobile: 18 dashboard tests and all 267 regression tests passed; full analysis clean. Existing authenticated-route SEO/robots behavior remains covered by the 12-test SEO suite. No production mocks or fixture values. | Passed (CI-safe; known unrelated admin teardown flake reproduced) | 2026-08-19 |
| 6.1 -- Permit-to-Work policy and domain contract | Visually inspected source PDF page 7 using the PDF workflow and confirmed it contains only the six permit types, eight workflow stages, and three suggested endpoints already present in the pasted text. Product owner approved the proposed production baseline by directing continuation. D-072 now locks risk, templates, worker acknowledgement, approval/signature chain, validity/expiry, exceptional lifecycle, online/offline boundaries, and the `permits.write`/`permits.approve` split. `git diff --check` passed. No schema or runtime code was introduced in this decision-only gate. | Passed (requirements gate) | 2026-08-19 |
| 6.2a -- Versioned tenant permit templates | Backend: Ruff passed and strict mypy reported no issues across 102 source files. The focused permit-template/RBAC/role/OpenAPI set passed 37/37; the full API regression passed 397 with 3 credential-only skips. Tests cover server-owned nested IDs and order, the complete role matrix, tenant isolation and cross-tenant 404 behavior, rejection of a cross-tenant approval role, duplicate nested-ID rejection, audited create/update/soft-delete behavior, version increments, and a stale update returning 409 while preserving current state. OpenAPI and pinned TypeScript Fetch/Dart Dio clients regenerated successfully. The Next.js production build compiled all 29 routes, and `flutter analyze lib test` reported no issues. The established tenant `cmp_feee017b83914cdd8323745e6359cc32` was reconciled successfully across all seven built-in roles, backfilling `permits.write` only to its intended role definitions. No production mock data or placeholder behavior was introduced. | Passed | 2026-08-19 |
| 6.2b -- Permit record foundation | Backend: Ruff passed and strict mypy reported no issues across 106 source files. The focused permit/template/RBAC/role/OpenAPI set passed 48/48; the full API regression passed 408 with 3 credential-only skips. New tests prove server-calculated initial/residual score and band, ordered checklist/approval snapshots, snapshot immutability after a template update, the complete read/write role matrix, tenant-isolated list and cross-tenant 404, same-tenant active-worker enforcement, duplicate-worker rejection, validity ordering, rejection when residual risk exceeds initial risk, template-type matching, facility/area/asset relationship validation, revision-conflict preservation, filtered cursor pagination, soft deletion, and create/delete audits. OpenAPI and pinned TypeScript Fetch/Dart Dio clients regenerated successfully. The Next.js production build compiled all 29 routes, and `flutter analyze lib test` reported no issues. No production mock data or placeholder runtime behavior was introduced. | Passed | 2026-08-19 |
| 6.3a -- Controlled submission and sequential approval/rejection | Backend: Ruff passed and strict mypy reported no issues across 106 source files. The focused permit/template/RBAC/role/OpenAPI set passed 53/53; the full API regression passed 413 with 3 credential-only skips. Tests prove required checklist completion and completion provenance, server-derived issuer signature/submission time, High/Critical residual-risk blocking, exact role-ordered approvals, out-of-order role denial, server-derived approval signatures, final transition to Pending Signatures, reasoned rejection back to Draft, resubmission resetting step states, and stale submit/approval revisions preserving current state. The first run caught two real contract gaps: Operations Manager lacked `permits.approve` despite being D-072's default first approver, and schema-level nonempty-list validation obscured the precise incomplete-checklist domain error; both were corrected and the same suite passed. OpenAPI and pinned clients regenerated successfully. The Next.js production build compiled all 29 routes; Flutter analysis reported no issues. Firestore reconciliation initially hit a transient DNS failure, then succeeded on retry for all seven established-tenant roles, including the corrected Operations Manager grant. | Passed | 2026-08-19 |
| 6.3b -- Worker acknowledgement/signature and online activation | Backend: Ruff passed and strict mypy reported no issues across 106 source files. The focused permit/template/RBAC/role/OpenAPI set passed 57/57; the full API regression passed 417 with 3 credential-only skips. Tests prove assigned-worker-only acknowledgement, preserved client mutation/time/device metadata, server-owned signer/receipt/signature provenance, exact-mutation replay as a no-write/no-revision-bump success, rejection of an unassigned worker and an excessively future client time, two workers colliding on a stale offline revision then succeeding after explicit rebase without lost data, activation blocked until every worker acknowledges, field-worker activation denied by RBAC, and successful activation only inside the server-evaluated validity window with authenticated activator/time. OpenAPI and pinned TypeScript Fetch/Dart Dio clients regenerated successfully. The Next.js production build compiled all 29 routes, and `flutter analyze lib test` reported no issues. The durable mobile queue that will call this replay-safe endpoint remains correctly scoped to Phase 6.4, not represented by runtime mock behavior here. | Passed | 2026-08-19 |
| 6.3c -- Automatic expiry and controlled terminal lifecycle | Backend: Ruff passed and strict mypy reported no issues across 107 source files. The focused permit/template/RBAC/role/OpenAPI set passed 61/61; the full API regression passed 421 with 3 credential-only skips. Tests prove reasoned Active→Suspended→Active with actor/time provenance, terminal revocation from Pending Signatures, terminal close from Active with attested closeout notes, due Active→Expired reconciliation on read with exactly one `permit.expired` audit attributed to `system:permit-expiry`, generic PATCH/DELETE rejection outside Draft, and a stale resume returning the current revision without overwriting suspension. `scripts/expire_permits.py` provides the production all-tenant recurring scheduler entry point while list/detail/transition reads independently reconcile due state. OpenAPI and both clients were regenerated; the Windows generator wrapper exceeded its command timeout after generation and left an orphaned `dart format` shell, which was terminated after verifying no Dart/Java worker remained. Direct `dart format lib test` then completed across 517 files with zero changes, proving generation had completed. The Next.js production build compiled all 29 routes; Flutter analysis reported no issues; `git diff --check` passed. | Passed | 2026-08-19 |
| 6.4a -- Admin permit-template management | Admin: 6 focused interaction tests pass, covering real template rendering and permit-type refetch, read-only mutation hiding and disabled row navigation, create payloads with ordered checklist controls and a real tenant role ID, edit requests carrying the loaded optimistic version, client validation preventing incomplete writes, and a branded no-access form that makes no role/template request. The first focused run passed 4/6 and exposed two test-visible issues: native required validation masked the intended ordered-item errors, and the no-access form started a role call before returning; the test now reaches the intended domain rules and the form's effect is permission-gated, after which 6/6 passed. Full Vitest passed 247 with 6 credential-only skips; its first run outlived the five-minute capture wrapper, so it was rerun with a ten-minute window and completed authoritatively. `next lint` reported no warnings/errors. The optimized Next.js build compiled 30 routes, including `/permits`, `/permits/templates/new`, and `/permits/templates/[id]`; each new route has protected metadata. The UI uses only generated contracts and live tenant role/template endpoints—no production mocks or placeholder runtime data. | Passed | 2026-08-19 |
| 6.4b.1 -- Admin permit register and draft creation | Admin: 6 focused tests pass, covering real permit/facility rendering, combined permit-type/facility API filters, live tenant facility/user/template/area/asset selectors, the exact typed create payload with `Date` instants and risk rows, residual-risk validation, and a `permits.write` no-access state that starts no protected resource request. Full Vitest passed 253 with 6 credential-only skips across 27 passing files; the two expected stderr exercises remain the established JSDOM download-navigation limitation and deliberate widget error-boundary test. `next lint` reported no warnings/errors. The optimized Next.js build type-checked and generated all 32 routes, including `/permits`, `/permits/new`, and `/permits/templates`, with protected metadata. The creation success path returns to the real register until 6.4b.2 delivers the detail route, so no broken or placeholder navigation is exposed. Production code consumes only generated contracts and tenant APIs; test fixtures are isolated to Vitest. | Passed | 2026-08-19 |
| 6.4b.2 -- Admin permit detail and lifecycle controls | Admin: 6 focused tests pass, covering immutable snapshot/risk/acknowledgement rendering, submission with selected checklist IDs plus loaded revision and issuer attestation, approval with server-signature attestation, mandatory rejection reasons, activation disabled until every assigned worker acknowledges, reasoned exceptional actions, and authoritative reload after a simulated revision conflict. Full Vitest passed 259 with 6 credential-only skips across 28 passing files; only the established intentional JSDOM download-navigation and widget error-boundary stderr exercises appeared. `next lint` reported no warnings/errors. The optimized Next.js build compiled, type-checked, and generated all routes with `/permits/[id]` as a protected dynamic route; shared first-load JS remains 102 kB. All transition calls use generated request contracts and live API state; no production mock, optimistic fake transition, or placeholder detail behavior exists. | Passed | 2026-08-19 |
| 6.4c -- Mobile offline permit workflow | Drift schema advanced v12→v13 with isolated complete-detail cache and FIFO acknowledgement outbox tables. Seven focused tests pass: five repository/worker cases prove durable server snapshots, atomic offline provenance, successful replay, exact-mutation already-applied recovery, and genuine conflict rebase preserving the original mutation ID/time/device; two widget cases prove cached assigned-list rendering and explicit attestation producing a visible pending offline action. Assigned refresh traverses all cursor pages and removes stale synced assignments, while conflicts display the authoritative server snapshot before discard/rebase. The complete mobile regression passed 274/274 and `flutter analyze lib test` reported no issues. Runtime code uses generated contracts and authenticated-user data only; unavailable adapters fail closed and fixtures remain test-only. | Passed (full mobile regression, local) | 2026-08-20 |
| 6.5 -- Active Permits dashboard KPI and Phase 6 closure | Backend focused dashboard/permit/OpenAPI coverage passed 37/37; Ruff and strict mypy were clean. Tests prove `permits.read` enforcement, tenant-scoped counts, active→expired reconciliation before aggregation, and a stable generated operation ID. OpenAPI plus pinned TypeScript Fetch/Dart Dio clients regenerated successfully. Admin dashboard coverage passed 17/17 and lint was clean; the isolated full suite passed (260 tests with 6 credential-only skips), and the optimized Next.js build compiled/type-checked all 32 pages. Mobile dashboard coverage passed 19/19, full analysis was clean, and the complete mobile suite passed 275/275. The CI-safe API regression passed 422 with four integration tests deselected. A separate credential-enabled attempt also reached 422 passes and three skips, but the remaining real Firebase auth test failed because DNS could not resolve `firestore.googleapis.com`; this is recorded as external network evidence rather than a product failure. No production mocks, placeholder values, or hand-written transport models were introduced. | Passed (CI-safe; live Firebase DNS unavailable) | 2026-08-20 |
| 9.1 -- AI report policy and domain contract | Reconciled source brief §13 against the completed inspection, safety, permit, work-order, RBAC, audit, and server-mediated storage boundaries. D-083 locks tenant snapshots, source-domain authorization, advisory AI editing, explicit human finalization, reviewed-inspection-AI gating, immutable finalized reports, and private PDF/DOCX/XLSX rendering. Tracker, context, architecture, and decision records agree on the Phase 9 decomposition; `git diff --check` passed. No runtime schema, endpoint, mock, or placeholder behavior was introduced. | Passed (requirements gate) | 2026-08-20 |
| 9.2 -- report backend foundation | Focused report/OpenAPI verification passed 10/10. Tests prove seeded inspection snapshot/model provenance, audited creation, source-domain permission enforcement, cross-tenant 404 isolation, human narrative edits, stale-revision rejection, explicit finalizer identity, finalized immutability, unreviewed-inspection-AI blocking, cursor pagination, and regeneration from a newer authoritative source snapshot with its own audit event. CI-safe API regression passed 430 with four integration tests deselected; Ruff and strict mypy were clean across 111 source files. OpenAPI and pinned TypeScript Fetch/Dart Dio clients regenerated successfully. Flutter analysis was clean and the optimized 32-page Next.js build compiled and type-checked. The focused admin API integration file contains two credential-only tests and was correctly skipped. The real Claude narrative call remains unverified without a live `ANTHROPIC_API_KEY`; CI exercises the identical provider-neutral service boundary with a structured fake. No artifact placeholder or public download behavior was introduced. | Passed (CI-safe; live Claude credential unavailable) | 2026-08-20 |
| 9.3 -- private PDF/DOCX/XLSX exports | Focused report/OpenAPI verification passed 11/11. Tests reject draft export, render and structurally parse all three finalized formats, verify byte sizes/private signed URLs, and prove one `generated_report.exported` audit event per success. Ruff and strict mypy passed across 112 source files. CI-safe regression passed 430 tests (406 non-integration outside the unrelated permit file plus 24 unaffected permit tests); the complete run additionally exposed only a live Firebase timeout and a pre-existing permit assertion hardcoded to yesterday's date. OpenAPI and pinned TypeScript Fetch/Dart Dio clients regenerated successfully. Flutter analysis was clean and the optimized 32-page Next.js build compiled/type-checked. Visual QA rendered all six pages: the native PDF and LibreOffice print renditions of DOCX/XLSX had no clipping, overflow, broken tables, or unreadable scaling. | Passed (CI-safe; live Firebase unavailable) | 2026-08-20 |
| 9.4a -- admin report library and downloads | Focused Vitest interaction coverage passed 3/3, proving real generated-report rendering, draft export gating, all three finalized format controls, tenant API type/status filters, a fresh export request, signed-link initiation, and success feedback. Next lint reported no warnings/errors. The optimized production build compiled and type-checked all 32 routes; `/reports` is now a real 1.98 kB protected route rather than a placeholder. Production code uses only the shared authenticated wrapper and generated report contracts; no mock report data or retained signed URL exists outside tests. | Passed | 2026-08-20 |
| 9.4b.1 -- admin report creation | The combined report UI suite passed 6/6. New tests prove a permitted inspection source is loaded and sent with the generated ID/title/type, executive summaries request and send no source, and absence of `reports.generate` renders a no-access state without any source read. Existing library/filter/export tests remain green. Next lint reported no warnings/errors. The optimized production build compiled and type-checked 33 routes, including the real `/reports/new` workflow. All runtime data comes from tenant APIs through generated contracts; snapshot assembly and advisory AI remain server-owned. | Passed | 2026-08-20 |
| 9.4b.2 -- admin report review and finalization | The combined report UI suite passed 10/10. Four detail tests prove frozen source rendering, revision-safe human title/narrative/risk edits, explicit source regeneration with the loaded revision, finalization disabled until human attestation, immutable finalized rendering with mutation controls removed, and confirmed audited draft deletion returning to the library. Next lint reported no warnings/errors. The first production build caught optional generated narrative arrays and the UI was hardened with empty-array handling; the rerun compiled/type-checked all 33 routes including dynamic `/reports/[id]`. Report routes now have explicit UI permission gates matching FastAPI. | Passed | 2026-08-20 |
| 9.4c.1 -- mobile report library and downloads | Flutter analysis reported no issues. Two focused report tests pass: controller coverage proves type/status filter requests and opaque cursor pagination, while widget coverage proves real generated report rendering, draft export gating, finalized controls, the exact export API request, and fresh signed-link launch through the injected opener. The affected shell regression passes with Reports resolving to the real module rather than its former placeholder. `url_launcher` 6.3.1 provides the production cross-platform external download handoff; signed URLs are not persisted. | Passed | 2026-08-20 |
| Maintenance -- mobile generated optional-query compatibility | Added an API regression reproducing the generated Dart/Dio asset request with blank optional filters; the endpoint returns all 11 seeded tenant assets instead of applying a blank-facility Firestore filter (29/29 API asset tests passed). Added a mobile transport regression proving the generated inspection request dispatches only the real `asset_id` and `limit`, omitting blank facility/status/inspector/date/cursor values that caused HTTP 422 on the asset-detail Inspections tab. Combined mobile API-service and inspection-detail regression: 29/29 passed; focused Flutter analysis reported no issues. | Passed | 2026-08-26 |
| Maintenance -- mobile asset-detail browser restoration | Reproduced the pasted web exception (`Null` cast to `String` at `app_routes.dart`) by loading `/assets/detail` without an in-memory route argument. Added coverage proving `/assets/detail?id=asset-1` restores and renders the real detail screen, while a missing ID renders the branded 404 without an exception. Full focused Assets screen suite passed 9/9; focused Flutter analysis reported no issues. | Passed | 2026-08-26 |
| Maintenance -- Flutter web Firebase SDK self-hosting | Reproduced startup failure while FlutterFire dynamically imported Firebase App/Auth/Storage from `gstatic.com`. Vendored the three pinned ESM modules, rewrote Auth/Storage to import the local App module, and registered them before Flutter bootstrap. The running localhost server returned HTTP 200 for all three files with expected byte lengths, and `flutter build web --debug` completed successfully (including the Wasm dry run). | Passed | 2026-08-26 |
| Maintenance -- FlutterFire Firebase JS version alignment | Replaced the initially vendored 12.16.0 modules with the exact 12.15.0 npm artifacts required by the current FlutterFire lockfile. Node loaded the complete local App/Auth/Storage ESM graph and reported `SDK_VERSION=12.15.0`, `getAuth` and `getStorage` as functions. Localhost served all three files with HTTP 200 and no 12.16.0 content. API `/health` returned 200/Firestore connected, and an Assets preflight from `http://localhost:8080` returned 200 with the correct allow-origin/method/header values. | Passed | 2026-08-26 |
| Maintenance -- asset-detail Work Orders Firestore index readiness | Reproduced the HTTP 500 path from `GET /api/v1/work-orders?asset_id=...` and found the committed indexes covered Assets/Inspections but omitted Work Orders. Added all four Work Order list composites plus a bounded locally sorted fallback. Work Order API suite passed 30/30; Ruff and strict mypy passed; Firestore index JSON parsed successfully; Assets/detail widget suite passed 10/10 including rendering real work orders on the tab. | Passed | 2026-08-26 |
| Maintenance -- Asset Media real camera capture | Replaced `ImagePicker(ImageSource.camera)` with a photo-only screen using the production `camera` preview and shutter flow; Gallery remains `ImageSource.gallery`. An injectable capture test proves the photo-only screen returns the captured result and exposes neither photo-gallery nor video-gallery actions. Combined media-capture and Assets/detail widget suites passed 17/17; focused Flutter analysis reported no issues. | Passed | 2026-08-26 |
| Maintenance -- camera initialization recovery | Expanded browser/device error classification for permission denial (`NotAllowedError`), busy/unavailable cameras (`NotReadableError`), missing devices, and unsupported constraints. The production view now stops its loader on failure and offers clean controller reinitialization through `Retry camera`. Focused media-capture suite passed 8/8 and focused Flutter analysis reported no issues. | Passed | 2026-08-26 |
| Phase 12.1 — public marketing site | Full admin suite passed 284/284 with 6 skipped (up from 277 — six new `src/marketing/marketing.test.tsx` cases plus one new sitemap assertion), covering anonymous landing render with both entry points, the authenticated header swapping to Go to dashboard at `/dashboard`, no CTA while the session restores, active-page marking, all three pricing plans with their own CTA, and footer reachability. SEO suite extended to prove robots.txt no longer blanket-disallows `/`, disallows `/dashboard`/`/settings`/`/verify-email`, lists no private prefix in the sitemap, and ranks the landing page 1.0. Route behaviour verified against a running dev server: `/`, `/pricing`, `/about`, `/login`, `/signup`, `/dashboard`, `/robots.txt`, `/sitemap.xml` all returned HTTP 200; the landing page server-rendered 113KB of real content with one `<h1>`, its canonical tag, `data-theme="light"`, and zero auth-splash markup, while `/dashboard` still rendered the splash under `noindex, nofollow`. `next build` compiled successfully; the build's lint and type gates still fail on pre-existing untracked/modified Phase 10.2 work (`digital-twin-viewer.tsx` raw hex colors, `asset-widgets.tsx` `AsyncStatus`), both untouched by this phase. No CI run or PR yet — not committed. | Passed | 2026-09-07 |
| Phase 12.2 — marketing visual pass | Full admin suite passed 286/286 with 6 skipped (two new cases beyond 12.1's six): the IntersectionObserver-absent fallback marks every `[data-reveal]` element `is-visible` so revealed content is never stranded at opacity 0, and the header progress bar's transform tracks scroll position (`scaleX(0)` on an unscrollable document, `scaleX(0.5)` at 1000 of 2000 scrollable pixels). `pnpm lint` reports no issues in any marketing file. Verified against a running dev server: `/`, `/pricing`, `/about` all HTTP 200, each with exactly one `<h1>`, a `mk-progress` bar, and 25–37 `data-reveal` sections; the compiled stylesheet contains every `mk-*` rule and resolves `--color-accent-500: #FB4402`. The pass also caught and fixed a silent Tailwind failure: `bg-surface/70`-style opacity modifiers compile to nothing against the CSS-variable color tokens, so those elements had no background at all — replaced with `color-mix` utilities and confirmed present in the served CSS. The same latent bug remains in 15 pre-existing app-shell files and was left untouched. A transient dev 500 (`CountUp` missing from the React Client Manifest) was a stale HMR manifest and cleared on restart. No CI run or PR yet — not committed. | Passed | 2026-09-07 |
| Phase 12.3 — marketing navigation fix and header layout | Two regression tests were written first and confirmed to FAIL against the pre-fix implementation before the fix landed: the jsdom fallback path (navigate `/` → `/pricing` → `/` under a persistent layout, asserting every `[data-reveal]` element carries `is-visible` and each page's `<h1>` is present) and the real browser path (a stubbed IntersectionObserver plus `getBoundingClientRect` returning `top: 5000`, so elements go through `observe()` instead of the fold shortcut, asserting the next page's nodes are observed by a fresh observer and revealed when they intersect). Marketing suite 11/11. Full admin suite: 288 passed, 6 skipped, with the single known `company-settings-page.test.tsx` Windows-only full-suite flake — that file passes 8/8 in isolation and an earlier full run in the same session passed 286/286, so it is unrelated to this phase. `pnpm lint` and `tsc` clean across every marketing file. Verified on a dev server started from a cleared `.next`: `/`, `/pricing`, `/about`, `/login`, `/dashboard` all HTTP 200 with zero server errors logged; `/about` served 25 and `/pricing` 29 reveal sections, each with one `<h1>`; the header renders as `grid-cols-[auto_1fr_auto]` with the site nav in the centre cell and a 40px icon-only toggle labelled "Switch to dark theme" carrying no visible text. The labelled "Light mode"/"Dark mode" pill no longer appears in marketing markup. Note for local dev: editing a `"use client"` module's exports while `next dev` runs can leave a stale React Client Manifest and produce a 500 naming the moved export; restarting the dev server clears it. No CI run or PR yet — not committed. | Passed | 2026-09-07 |
| Phase 12.4 — landing page rebuilt around the product | Full admin suite passed 292/292 with 6 skipped and no flake this run (the known Windows-only `company-settings-page` flake did not fire). Marketing suite 14/14, with three new cases guarding the claims the page now makes: the hero exposes one `role="img"` whose label describes an illustration and names Tank Farm A rather than implying a screenshot; every `RecordChain` step renders its heading and gating permission, asserting both `work_orders.write` and `work_orders.close` appear so the separation-of-duties claim cannot silently disappear; and the role matrix renders all seven `rowheader` role keys with their scope text, pinning `field_inspector` at 14 keys. `pnpm lint` and `tsc` clean across every marketing file. Verified on a dev server: `/`, `/pricing`, `/about` all HTTP 200 with zero server errors; the served landing markup contains all six hero animation hooks (`mk-device`, `mk-conn`, `mk-queue-row`, `mk-packet`, `mk-received`, `mk-wire`), all four record ids, all seven role keys, and the new `<h1>`; the compiled stylesheet carries every keyframe (`mk-float`, `mk-conn-cycle`, `mk-queue`, `mk-queue-dot`, `mk-packet`, `mk-received`, `mk-wire`, `mk-chain-line`) plus five `prefers-reduced-motion` blocks giving the hero an explicit rest state. `CountUp` was deleted with the figure strip it served, and no reference remains. No CI run or PR yet — not committed. | Passed | 2026-09-07 |
| Phase 12.5 — marketing action-cluster restyle | Full admin suite passed 293/293 with 6 skipped, no flake. Marketing suite 15/15 with one new case pinning the header design contract: the header primary is `rounded-full` and has no `svg` arrow child, every header "Log in" link (desktop and the compact-strip one) carries no border class, and body `/signup` CTAs outside the header still render their arrow — so the arrow default cannot silently flip either way. The existing toggle case was extended to assert no resting border alongside its empty text content. `pnpm lint` and `tsc` clean across every marketing file, including the `#FB4402` contrast note (the hex ban applies to literals and template elements, not comments). Verified on a dev server: `/`, `/pricing`, `/about` all HTTP 200 with zero server errors; served markup shows the accent pill as `rounded-full ... px-7 py-2.5 text-body text-accent-ink` and the toggle as a bare `size-9 rounded-full` with hover-only background. Confirmed the header's CTA and bare login are absent from server markup by design — they are gated on settled auth status — which is why their contract is asserted in jsdom rather than against the HTML. No CI run or PR yet — not committed. | Passed | 2026-09-07 |
| Phase 13.1–13.2 — plan catalog, entitlements, published pricing | API suite passed 474/474 with 3 skipped, including a new `tests/test_billing_entitlements.py` at 34/34. Those cases pin every published price and quota against requirements §22.1 (a typo here is a commercial defect), assert the annual total is exactly twelve months at list, assert AR inspection / permits / work orders are Operations-and-above while VR / SSO / audit-export are Enterprise-only, and assert features only ever grow up the tier order so an upgrade cannot silently remove access. Failure paths covered: `incomplete` and `canceled` grant nothing, an `unassigned` tier grants nothing even when stamped `active`, unknown status text fails closed, an unentitled company reports a zero quota rather than unlimited, and `past_due` deliberately still grants access. The seed contract test was updated for the six new `Company` fields. Admin suite passed 294/294 with 6 skipped (up from 292 — two new pricing cases asserting the tier order, allowances, per-tier trial CTA hrefs carrying the plan, and the published cent values). `ruff check app/billing` and `mypy app/billing` clean; admin `pnpm lint` and `tsc` clean apart from the pre-existing untracked Phase 10.2 files. Stripe itself is unverified — no test keys yet, so no Price, Checkout session, or webhook has been exercised. No CI run or PR yet. | Passed (Stripe deferred) | 2026-09-08 |
| Phase 13.3 — Stripe gateway, price sync, webhook reconciliation | API suite passed 493/493 with 3 skipped, including a new `tests/test_billing_stripe.py` at 19/19 against a fake gateway. Those cases pin the lookup keys as a wire contract, assert checkout stamps the 7-day trial and our company/tier metadata, and cover the reconciliation invariants: state comes from a live re-read and not the event body (a body claiming `enterprise`/`canceled` loses to a live read of `starter`/`active`), replaying an event writes identical state and emits no second audit, cancellation removes entitlement, missing `fev_tier` metadata keeps the existing tier, irrelevant events are ignored with zero Stripe reads, and an event with no company reference resolves as `unresolved` rather than erroring so Stripe does not retry forever. The OpenAPI operation-id contract was updated for three new billing operations (the webhook stays out of the schema). `ruff` and `mypy` clean across `app/billing`, the router, the sync script, and both test modules. **Real-creds Stripe test-mode checks:** `stripe_sync` created 4 Products and 8 Prices, a re-run reported 0 changes (idempotent), and `--verify` read all eight lookup keys back at exactly the catalogued amounts ($1,147.99/mo and $11,975.88/yr through $34,497.99/mo and $359,975.88/yr). Six live Checkout Sessions were opened (Starter/Operations/Enterprise × annual/monthly) and read back with the correct price, lookup key, `mode=subscription`, `payment_method_collection=always`, `client_reference_id`, and tier/interval metadata; each showed `amount_total=0`, confirming nothing is billed during the trial. A live subscription created at `TRIAL_DAYS` reconciled through `snapshot_from_subscription` to `status=trialing`, tier `operations`, interval `annual`, and a trial exactly 7 days out; the test customer and subscription were then deleted. Not yet exercised: an end-to-end completed checkout (needs a browser and a card) and the signed webhook round trip (needs `STRIPE_WEBHOOK_SECRET` from `stripe listen`). No CI run or PR yet. | Passed | 2026-09-08 |
| Phase 13.4 — two-step signup with Stripe checkout | API suite 496/496 with 3 skipped (up from 493 — three new cases pinning the D-092 carve-out: an unverified company admin is admitted to checkout, the `company.settings` permission is still required, and entitlements resolve for an unverified admin so the completion page can poll). Admin suite 305/305 with 6 skipped (up from 292), including a new `src/billing/signup-billing.test.tsx` at 11/11: every catalogued plan renders with the server's trial length and cents-derived prices, the `?plan=` carried from the pricing page preselects, the interval toggle swaps the displayed price, the chosen tier/interval reach `createCheckoutSession` and the browser leaves via a full navigation rather than a router push, a failed session says "nothing was charged", an already-subscribed company skips the step, a catalog failure offers a retry instead of an empty picker, and the completion page polls until the webhook lands, keeps polling through a transient API failure, routes an unverified admin to verification, and after a bounded wait offers both "check again" and "continue anyway" rather than stranding the visitor. `ruff`, `mypy`, and `next lint` clean across all new billing code. Contracts regenerated: `openapi.json` gained the three billing paths and `BillingApi` plus six models were generated for the TypeScript and Dart clients. Two findings worth recording: the committed `openapi.json` was stale against the working tree (the export added Phase 9-11 surface that had never been exported), and `tsc` on the admin app has **68 pre-existing errors across ~20 files** of in-flight work — optional `nextCursor` typing and a `getDashboardReportsSummary` wrapper method that `client.ts` never defined — none in any Phase 13 file, and verified pre-existing by diffing the generated models against HEAD. Not yet exercised: a completed browser checkout and the signed webhook round trip (still needs `STRIPE_WEBHOOK_SECRET`). No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.5 — plan-aware admin shell and dashboard | Admin suite 323/323 with 6 skipped (up from 305). New `src/billing/plan-card.test.tsx` at 15/15 covers the card (plan name, status, trial countdown, 50%/40% usage bars from real limits, unlimited rendered as text with no progressbar, an unknown count as a dash not a zero, past-due warning that does not imply lockout, "no active subscription" copy, a loading state that does not claim a plan, and an error state that says access is unaffected) and the gate itself (grants only what the feature list contains, refuses an omitted feature, treats an ungated item as universal, fails closed while loading and on error with `data-status="error"` asserted, and refuses everything when `isEntitled` is false regardless of the list). Three new `app-shell` cases prove the gates are independent: with the full `company_admin` permission set but only the base plan, Work Orders and Permits are absent while Assets, Inspections, Safety, Reports and Documents remain; the richest plan restores them; and an empty feature list leaves only ungated Dashboard. Two `visibleNavGroups` unit cases pin each gate in isolation. Four existing harnesses (shell, dashboard, auth-experience, billing) now supply a subscription because `useSubscription` throws without a provider — deliberately fail-closed. One assertion changed for a real behaviour change: the dashboard's "7" count went from 2 to 3 because the plan card now also shows seats used against the seat cap. `pnpm lint` clean apart from the pre-existing `digital-twin-viewer.tsx` hex-colour failures. Not yet exercised: a completed browser checkout and the signed webhook round trip (still needs `STRIPE_WEBHOOK_SECRET`); mobile plan-gating is 13.6. No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.6 — plan-aware mobile app | Mobile suite 310/310 (up from 292), including a new `test/billing/subscription_controller_test.dart` at 18/18. The controller cases pin the same invariants as the portal's: grants only the listed keys, treats a null feature as universal, fails closed both before the plan has loaded and after a failed load, refuses everything when `isEntitled` is false regardless of the list, keeps a `past_due` company entitled, notifies exactly once on load, accepts an injected initial snapshot without loading, and survives a load that resolves after `dispose()`. The navigation cases prove the gates are independent — with every permission but only the base plan, Work and Permits disappear while Assets/Inspections/Safety remain; the richest plan restores them; **Work drops out of the bottom bar**, so gating reaches `primaryDestinations` and not only the More sheet; an unloaded plan leaves only ungated Home; and the default predicate is proven equivalent to allow-all for pre-Phase-13 callers. Three `SessionSubscriptionScope` widget cases cover loading and exposing the plan, `maybeOf` returning null outside the scope, and a reload when the signed-in company changes so one tenant's plan cannot leak into another's shell. `flutter analyze` reports only the 5 pre-existing `withOpacity` deprecation infos in `logo.dart`. Adding `getSubscription` to `ApiContract` broke 18 test fakes at compile time (one used a multi-line `implements` clause and needed a separate patch); all now return the shared fully-entitled fixture. Not yet exercised: a completed browser checkout and the signed webhook round trip (still needs `STRIPE_WEBHOOK_SECRET`). No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.7 — entitlement gate applied to every module router | API suite 521/521 with 3 skipped (up from 496). Applying the gate first produced **261 failures** — every module test hit `RuntimeError: Event loop is closed`, because the new dependency resolved the caller's company through a real Firestore client the suite does not have. A shared `tests/conftest.py` now defaults every test to an entitled Enterprise tenant, which is the honest shape: tests about permissions and domain rules should not have to think about billing. New `tests/test_billing_gate.py` at 25/25 pins the server half: each of the three Operations-and-above routers returns **402 `plan_upgrade_required`** on Starter with `required_tier`/`required_tier_name` naming a real plan; the same routes are reachable on Operations; all six base modules stay open on Starter; Field is gated identically to Starter (it buys capacity, not capability); an `incomplete` company is refused every module with `subscription_inactive`; a cancelled company loses access; a **past-due company keeps working**; a trialing company has full access; and six account/billing surfaces (`/auth/me`, `/billing/subscription`, `/billing/catalog`, `/dashboard/summary`, `/users`, `/company`) stay reachable with no active subscription so an unpaid customer can still reach checkout. Two catalog-consistency cases pin that Operations adds exactly `ar_inspection`, `permits`, `work_orders` over Starter and that Field's feature set equals Starter's. **A test-helper bug was caught by these tests**: the first `entitlements_for` granted the plan's features regardless of status, so a cancelled company looked entitled; it now routes through `resolve_entitlements`. `ruff` and `mypy` clean across all Phase 13 files; the remaining `facilities/service.py` mypy errors and `main.py`/`seed.py` E501s are pre-existing in uncommitted Phase 9-11 work. No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.8 — browser signup fixed at the generator | Admin suite 325/325 with 6 skipped (up from 323). Diagnosis: the same registration payload returned **422 from the browser and 201 from curl**, which located the fault in the client rather than the model. Reading the generated serializer showed `...value,` spreading the camelCase source object alongside the snake_case wire names, so the body carried `companyName` beside `company_name` and `StrictModel`'s `extra="forbid"` rejected it. Only 3 of 185 models were affected and they were exactly the three declaring `additionalProperties: false`; comparing against HEAD confirmed the spread was **pre-existing and not introduced by the Phase 13.4 contract regeneration**. Fixed in `gen-clients.mjs`, which reported "Removed the additionalProperties spread from 3 TypeScript model(s)" and left 0 remaining. New `src/api/serialization.test.ts` (2/2) asserts the registration body is exactly the four declared wire fields with no camelCase key, plus a contrast case proving a non-strict model was always correct. **The guard was verified against the real defect**: reintroducing the spread failed the test with `expected [ 'companyName', 'company_name', ... ]`, then the fix was restored and it passed. End-to-end, a registration with the exact body the fixed client now sends returned **201** with a real company id and `role_key: company_admin`. Note for whoever runs it next: the admin app resolves `@fev/api-client` to the generated source through tsconfig paths, so a running `pnpm dev` holds the old module in memory and must be restarted. No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.9 — plan-aware widgets, sidebar plan footer, subscription page | Admin suite 330/330 with 6 skipped (up from 325). The reported defect was a 402 toast on the dashboard of a Starter tenant: `work-order-widgets` calls `listWorkOrders` on the gated router, which the nav gate never covered. Two new `widget-registry` cases pin the fix — a widget whose module the plan omits is hidden **even with the permission**, and no gated widget renders until the plan is known (fails closed rather than flashing). Extending the gate made 14 tests across four widget suites fail with `useSubscription requires SubscriptionProvider`, which is the intended fail-closed behaviour; all four now supply a fully-entitled fixture, and `SubscriptionProvider` was changed to read `AuthContext` directly so a seeded provider needs no Firebase gateway. Verified against the live tenant recovered in the previous step: `starter`/`trialing`, `work_orders` false, `assets` true with a 150 cap. `next lint` clean and `tsc` clean across every new file. Not covered by automated tests: the visual placement of the shared `ThemeToggleIcon`, the sidebar footer layout, and the scrollbar styling — all confirmed only by reading the rendered classes. No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.10 — documents transport and page props | Admin suite 332/332 with 6 skipped (up from 330). Diagnosis of "documents don't load" ruled the API out first with real tokens: `GET /api/v1/documents` answered **200 with seeded documents** for the Enterprise admin and **200 with an empty list** for the Starter tenant, while `/work-orders` correctly answered 402 — so the failure was client-side, and `grep` found `listDocuments` absent from `FevApiClient` entirely. New `documents-client.test.ts` (2/2) pins both methods: the list request carries its filters in the query string, and create posts wire-named fields with no camelCase leak (the D-095 trap). The existing documents page test passes again after its fixture was corrected from wire names to the client's `CurrentUser` shape and given the context fields it actually has — it had invented `user`/`loading`/`login`/`logout`. `tsc` is now clean across `src/documents`, which it was not before. `next lint` reports only the pre-existing `digital-twin-viewer.tsx` failures. Not covered by tests: the sidebar edge handle's placement and the `PageLoader` visuals, confirmed only by reading rendered classes. No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.11 — branded page loader | Admin suite 332/332 with 6 skipped, unchanged: the four screens that swapped `Spinner` for `PageLoader` are asserted on their copy and roles rather than on the loader element, so the existing cases carried over. Confirmed both clients previously used `LogoLoader` only on the auth splash (`route_guards.dart` and `SplashScreen`), so "the same as mobile" is now literally true for admin page waits. `next lint` reports only the pre-existing `digital-twin-viewer.tsx` failures and `tsc` is clean across the changed files. Not covered by tests: the loader's appearance and the restored progress track — confirmed by reading the compiled class names only. No CI run or PR yet. | Passed | 2026-09-10 |
| Phase 13.12 — nav skeleton and mobile-matched loader | Admin suite 333/333 with 6 skipped (up from 332). One new `app-shell` case pins the reported defect: with the plan still loading the primary nav contains **zero links** and shows pulse placeholders, so it cannot render a short list and then grow. Writing it surfaced that the existing `navLabels()` helper uses `getAllByRole`, which throws on zero links — the assertion had to use `queryAllByRole`, and the throw was itself confirmation the skeleton had replaced the list. The loader rewrite is covered indirectly: `SplashScreen` and `PageLoader` cases assert on the status label and `role="status"`, both unchanged, and the whole suite stayed green through the swap. Not covered by tests: the loader's appearance against the Flutter original — compared by reading both implementations side by side (aura size, arc stroke, disc shape, label weight and tracking) rather than visually. No CI run or PR yet. | Passed | 2026-09-10 |

| Client feedback round (2026-09-10) | Backend: ruff clean, mypy clean across 153 source files, 595 passed / 3 skipped. Admin: `next build` compiles, `next lint` clean, `tsc --noEmit` 0 errors, 358 passed / 6 skipped. Mobile: `flutter analyze` reports only the 5 pre-existing `withOpacity` deprecations, 317 passed. Contracts regenerated with no drift. New coverage: 3 for derived inspection titles, 3 for the title backfill, 6 for the real PyAV decoder plus 4 for the video analysis path, 18 for notifications and 2 for the work-order notification hook, 20 for VR training (including 3 proving a tampered client cannot self-score a multiple-choice step), 8 for the VR training page, 7 for mobile notifications, 3 for the notification bell, 2 for the safety failure state, 3 for the 3D deep links, 2 for inspector display names, and 4 for the digital twin's real-scene path. The safety and 3D navigation regressions were confirmed to fail against the pre-fix code. | Deferred: AR measurement on physical hardware (D-063) and the live `ANTHROPIC_API_KEY` vision call — both need hardware or credentials, not code. |

| Live Claude vision verification (2026-09-10) | `scripts.verify_ai_vision_live` run against the real Anthropic API with the key in `apps/api/.env`, model `claude-sonnet-5`. **Photo:** 2 findings — `rectangle/corrosion` conf 0.85 at (0.38,0.40)-(0.82,0.62) and `point/leak` conf 0.40 — risk `medium`, all coordinates within 0-1 and confidences within bounds. **Video:** a 2s clip sampled to 4 frames at 0.25/0.75/1.25/1.75s returned 1 finding, `rectangle/corrosion` conf 0.85 carrying frame offset 0.25s, summarised as "consistent ... across all sampled frames, with no significant change in extent". That single de-duplicated finding is the empirical confirmation of D-096: sending the frames as one request makes a recurring defect report once instead of four times. This closes the Phase 7.10 open item — the vision path had never run against a live key until now. | The script asserts the response contract (tool call returned, parses to `AiAnalysisResult`, findings within documented bounds), never Claude's judgement of a synthetic image. Deliberately a script, not a test: it costs real credits and must never run in CI. |

| Second client-feedback pass (2026-09-11) | Backend: ruff clean, mypy clean across 155 source files, 595 passed / 3 skipped. Admin: tsc 0 errors, lint clean, 9 new global-search tests and 2 new safety deep-link tests. Mobile: `flutter analyze` reports only the 5 pre-existing `withOpacity` deprecations, 317 passed. Contracts regenerated. Coverage added: every search category returns its records, a QR code resolves to the asset it labels, hits open records rather than list pages, one failing module does not blank the palette, the safety page opens `?reportId=` (with a negative case proving it does not fire without one), and the seed's permit counts are pinned so an empty register cannot silently return. | **Live SES delivery verified**: `scripts.verify_notification_email_live` sent a branded notification from `no-reply@flacronenterprises.com` via `us-east-1`; SES accepted the message (2.3 KB HTML, inline logo). Push still needs a device token registered from a real handset. |
| Signup flow correction (2026-09-14) | Backend: `ruff check .` clean, `mypy` clean across the billing package, router and API models, **608 passed / 3 skipped** (up from 598). Ten new cases in `tests/test_billing_stripe.py` cover the confirmation path: a returning checkout grants the purchased tier with **zero webhook involvement**; state still comes from a live `retrieve_subscription`, so a body claiming one tier loses to a live read of another, exactly as the webhook path does; confirming twice writes identical state and emits one audit entry, so a retry racing a webhook is harmless; a session Stripe has not yet attached a subscription to resolves `pending` and writes nothing; a session whose `client_reference_id` names another company is refused **before any subscription read**, which is the check that stops a pasted session id buying someone else's plan; a session with no reference at all is refused for the same reason; and an expired session is reported as such. Three mapping cases pin the Checkout Session shape (bare and expanded `subscription`, and the metadata fallback for `client_reference_id`). The OpenAPI operation-id contract gained `confirm_checkout_session` — that test failed first, which is what caught the omission. Admin: `tsc --noEmit` 0 errors, `next lint` clean apart from the pre-existing `digital-twin` exhaustive-deps warnings, **374 passed / 6 skipped** across 42 files. `signup-billing.test.tsx` rewritten to 20 cases: the picker announces **Step 3 of 3** and no longer repeats a verification request already satisfied, preselects both plan and interval from the parked pricing choice, lets a `?plan=` on the URL override a parked one, clears the parked choice once checkout starts, forwards an already-subscribed company to the dashboard rather than to verification, and shows a non-admin an "ask your Company Admin" state instead of a picker whose button would 403. For completion: the exact session Stripe returned is confirmed and `getSubscription` is **never called**, `pending` is retried rather than failed, a transient error is retried, a 400 is **not** (one call, then the support message with the checkout reference), the return URL without a session id still falls back to the read model, the workspace opens by itself once live, and — pinned explicitly — **no "Continue anyway" button exists and `/dashboard` is never reached from the failure state**. `auth-experience.test.tsx` gained three: registration now lands on `/verify-email` and asserts it is *not* `/signup/plan`; the branded SES sender is preferred and the provider's is not called; and it falls back to the provider when SES answers 503. One more pins the gate: an authenticated admin with an unentitled subscription reaches the plan step, not a shell — the harness now mirrors production by wrapping its protected branches in `RequireSubscription`. Contracts regenerated; `openapi.json`, `BillingApi`, and two models added to both the TypeScript and Dart clients. Not exercised: a real browser checkout against live Stripe keys, and the webhook endpoint itself — the confirmation route removes it from signup's critical path, but renewals and cancellations still depend on it being correctly configured in the Stripe dashboard. | Passed (live Stripe deferred) | 2026-09-14 |
| Live signup + Stripe checkout verification (2026-09-14) | Real-creds run of the whole corrected flow against the live Firebase project and the live Stripe account in **test mode** (`sk_test_`), driven by `scripts/verify_signup_checkout_live.py` with Stripe's hosted Checkout page completed by a real browser and card `4242 4242 4242 4242`. Two full cycles, both green, every artefact cleaned up afterwards (subscriptions cancelled, customers deleted, Firebase users and companies removed, plus 3 orphans swept from the failed early attempts). **Proved in order:** a new admin starts `email_verified=false`; `/assets` answers **403 while unverified**, so the verify step is a real gate; after verifying, `/billing/subscription` reports `is_entitled=false`, which is exactly what sends the admin to the plan step; `POST /billing/checkout` opens a real session; **confirming before paying returns `outcome=pending`, not a failure** — the branch that used to look like a broken purchase. The browser then paid, and the redirect Stripe actually sent was captured verbatim by a listener on :3000: `/signup/complete?session_id=cs_test_b1qBwLFo...`, matching the session created — which is the substitution the whole confirmation mechanism rests on. Settling then gave `starter/trialing` with **no webhook involved at all**, confirming twice returned byte-identical state, `/assets` went 200, `/work-orders` stayed **402 plan_upgrade_required** on Starter, and a second real company confirming the same session id was refused **400 session_mismatch** with its own plan still unentitled. **Two defects found that no existing test could see.** (1) `_subscription_response` had been left calling itself, so `POST /billing/checkout/confirm` answered **500 RecursionError** on every request while the service beneath it was correct and all ten of its unit tests passed — nothing exercised the billing routes as HTTP. New `tests/test_billing_routes.py` (8 cases) closes that: the confirm route's response, that its echoed plan is byte-identical to the read model, that the company is re-read *after* confirming (the injected entitlements are stale by construction), `pending` as a 200, a mismatch as 400 not 500, the `company.settings` gate, and an empty session id as 422. **The guard was verified against the real defect**: reintroducing the recursion failed 5 of the 8, then the fix restored them. (2) A *refused* SES send escaped as an unhandled **500** — `ses_configured` only checks the AWS keys are present, and this machine's had been rotated, so SES answered `InvalidClientTokenId`. Now `EmailDeliveryError` carrying the provider's code, mapped to **502 `email_delivery_failed`**, kept distinct from the 503 for an unconfigured deployment; one new case in `test_auth.py` pins it. The live run exercised that path for real and the client's documented fallback to the provider's own sender is what keeps signup working through it. Backend after both fixes: **617 passed / 3 skipped** (up from 608), `ruff` clean, `mypy` clean across 12 source files. | Passed | 2026-09-14 |
